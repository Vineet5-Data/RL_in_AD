# Project Summary — Map Matching + Path Prediction (consolidated 2026-07-11)

> **The single active research doc.** Consolidates four docs now retired to `archive/` (full record, unedited): `archive/critique_and_next_steps.md` (governance, gates, run history) · `archive/methodology-comparison.md` (literature synthesis + all empirical results) · `archive/practical_roadmap.md` (product roadmap) · `archive/run2_capacity_diagnosis_and_scaling_review.md` (capacity diagnosis + scaling literature, full citations).

## 1. What this is

Practical project: a deployable map-matching + path-prediction system trained **label-free** on raw fleet GPS (Porto; T-Drive for OOD). Architecture vocabulary: P1a transformer/attention encoder, P1b learned semantic vectorization, P2 latent-world-model RL, P3 label-free training. Success = product metrics (accuracy, latency, OOD, retraining cost).

## 2. Current best system

| Capability | Component | Number |
|---|---|---|
| Offline (batch) matching | **Hybrid Viterbi** — WM road-head log-prob + NK Gaussian emission, NK transitions | **0.8679** tolerant Hit@1, n=500 (2026-07-26, P2 32×32 ckpt @58% trained — new project best, was 0.8655 on 16×16) |
| Online (streaming) matching | WM road head + NK emission/transition ("hyb+topo", `matcher.py`) | **0.8287** match@1, n=500 (2026-07-26, P2 32×32 @58% — new project best, was 0.8122 on 16×16; jumps% not re-measured this run) |
| Path prediction | WM prior rollouts | beats HMM β=20 at all horizons |
| No-GPU fallback | NK-HMM Viterbi β=20 | 0.8447 offline / 0.5918 online |

Model: 2.6M-param decoder-light RSSM (h=512, 16×16 categorical latent), trained 60k steps on 200k Porto trajs (~8.2M fixes), 2 chained Kaggle T4 sessions.

## 3. How we got here (run history, compressed)

- **Track A (Runs 1–3, reconstruction RSSM):** faithful DreamerV3-style port posterior-collapsed across 4 experiment rounds — `h` freeloads on the teacher-forced road-embed channel via the reconstruction decoder (mechanism per Dai/Wang/Wipf ICML 2020). Pre-registered gate R2 failed at all checkpoints → hard stop. Honest negative, closed.
- **Track B (NK-HMM):** classical baseline, 0.8447 offline. Porto-learned semantic emission FAILED to beat raw geometry (in-domain and OOD −21.9pp on T-Drive) — recurring lesson: learned signal rarely beats geometry head-on here.
- **Track C (decoder-light WM, research2):** removed MDN decoder, GPS head z-only, road CE primary. Collapse eliminated (KL holds 8–9+ nats). Run 1 (15k steps) hit a curriculum/LR bug (L=64 phase trained at ~0 LR). **Run-2-XL** (60k steps, fixed schedule): match@1 0.695→**0.7684** (+7.3pp from schedule fix alone), hybrid Viterbi 0.8560→**0.8655**. Both matching gates PASS, prediction gate marginal, KL healthy. Per pre-registered exit rule: **WM track closed at 60k — no round 3** of the same lever.
- **Key structural finding:** WM emission alone loses to geometry (0.7684 vs 0.8447 offline) but is *complementary* — hybrid (WM + NK emission) is the first learned-signal win over pure geometry in the project. NK transitions over pure WM emission actively hurt (0.7268); the win comes from adding, not replacing.
- **RL actor (stage3):** underperforms the supervised road head for matching (0.593 vs 0.695) — physics-reward optimum ≠ nearest-road target. Reopened 2026-07-26 (see below) — track now CLOSED with stronger evidence than the original bounded attempt.
  - **Root-cause diagnosis (2026-07-26).** Reward-shaping mismatch confirmed: `r_topo`(w=2.0)/`r_head`(w=0.5) dominate `r_dist`(range ~[-5,0]) — actor optimizes topo/heading smoothness, not the eval metric's pure `d_perp` proximity. Reweight+refit frozen reward head (`head_only` config, local-CPU 7-config sweep + Kaggle CPU confirm): 0.593→0.6467 — helped, didn't close the gap to road-head (0.7653-0.8371).
  - **Early-stop / RL-unlearning probe (2026-07-26, local CPU, `~/.claude/jobs/.../actor_earlystop_probe.py`, reuses `ckpt/tune_head_only.pt`'s already-refit reward head, 20k-traj cache, 80-traj proxy eval, Run-2-XL 16×16 WM).** Tested whether match@1 peaks partway through RL fine-tuning before degrading. Result: **no sweet spot exists — step 0 (pure road-head warm start, zero RL) IS the peak** (match@1 0.7744, ≈ road-head ceiling 0.7830). From step 1, **monotonic decline, no recovery**: 0.7623(25)→0.7289(50)→~0.70(75-225)→~0.65-0.66 flat floor(300-800). Entropy falls in lockstep (1.42→0.48), no plateau-then-stable pattern. **Diagnosis: not noisy convergence to a plausible-but-worse optimum — systematic drift away from a known-good init from the first gradient step, the signature of reward-model overoptimization (Goodhart) against the frozen, imperfect learned reward head, not just a reward-weighting problem.** Early stopping cannot rescue this (there's nothing to stop early at — the actor's true optimum under this training scheme is "don't train it").
  - **Verdict: actor track CLOSED, stronger evidence than the original bounded-attempt rule required.** `matcher.py`'s existing choice (road-head decode, no RL) is now doubly justified: the RL actor's own training dynamics prove untrained IS optimal under this scheme. A real fix (not more tuning) would require changing the mechanism — trust-region/KL-constrained fine-tuning against the road-head, or a better (non-overoptimizable) reward model — bigger investment, gated behind an actual product need for sequential decision-making (§7), none currently exists.
- **Online topology experiment (2026-07-11, zero training):** base online filter jumps to a non-connected road at **14.9%** of transitions (the GIF artifact, now quantified). Adding NK transition+emission terms to the online argmax (`hyb+topo`, the streaming analog of the offline hybrid recipe): match@1 0.7690→**0.8122** and jumps→**5.86%** — dominates base on both axes. Pure d_perp hybrid scores 0.9706 but that is proxy-metric inflation (tolerant Hit@1 rewards nearest-candidate by construction) and still jumps 13.1%; topo-soft alone hurts (0.638, consistent with the offline wm-viterbi finding); topo-hard gives fewest jumps (5.29%) at −1.5pp. Raw: `.worktrees/research2/eval_online_topo.{py,txt}`. **Wired into `matcher.py` same day** as the trained-city online default (`mode="base"`/`"nk"` remain available); smoke re-passed all bars.
- **First ground-truth eval (2026-07-11, Kubicka benchmark, zenodo 57731 CC-BY-SA):** NK-HMM on all 100 worldwide GT tracks (their networks, 1 Hz): route-length error median **3.9%** / mean 6.5%, directed point acc 0.912 — published-classical ballpark, bar B1 PASS. On 8 clean tracks with OUR OSM graphs (15 s resample, zero-shot Porto WM): GT point-on-route acc **nk 0.970 / hybrid 0.970 / online-hyb+topo 0.976** — hybrid TIES pure geometry OOD on real GT (consistent with the T-Drive proxy OOD result; the learned emission neither helps nor hurts off-Porto). Wiring pitfalls found: benchmark arcs are directed with reverse twins (98%), files are lon-lat (x y) order. Raw: `.worktrees/research2/eval_gt_kubicka{,_wm}.{py,txt}`, `gt_benchmark_spec.md`. **Publication verdict:** the "hybrid beats geometry" claim stays proxy-only in-domain (no public Porto GT) and ties OOD → not publishable as-is; the posterior-collapse mechanism finding remains the viable workshop paper; Phase 2 (label-free training on a GT-benchmark city's public raw GPS, then in-domain GT test) is the remaining path to a strong claim — gated, not started.
- **OOD test (2026-07-11, roadmap ② DONE):** Run-2-XL WM zero-shot Porto→T-Drive/Beijing (500 trajs, final ckpt): online match@1 **0.6089** (−16.0pp vs in-domain 0.7684); offline hybrid **0.7871** < pure NK **0.7955** — the in-domain +2.1pp learned-emission win flips to −0.8pp OOD. NK geometry robust across cities; WM emission is city-specific. KL healthy OOD (no collapse); prediction hit@5 0.536 degrades less than matching. Same lesson as Track-B semantic emission OOD failure, milder. **Consequence: no city-agnostic claim; hybrid = trained-city mode, NK = fallback on new cities until retrained.** Raw: `.worktrees/research2/eval_ood_tdrive_{roadhead,offline_viterbi}.txt`.
- **Phase 2 — per-city label-free retraining on Silesia public GPS: FAILED, both iterations. Stop rule fired 2026-07-13.** The remaining "strong claim" path named in the 2026-07-11 bullet above was tested and closed:
  - *Iteration 1* (naive retrain, 20k steps, clean run): W1 held-out proxy retrained 0.6456 vs zero-shot 0.7376 (**−9.2pp**); GT@30s pooled retrained hybrid 0.7375 vs zero-shot 0.8877 (**−15pp**), below pure geometry. Diagnosed cause: modality contamination (18.1% of Silesia trajs are walker/rail-speed, vs 8.8% Porto) — the road head learned off-road emission a taxi WM never sees.
  - *Iteration 2* (speed-filtered [2,40] m/s → 6,012 trajs, same recipe, ckpt `ckpt_car/stage2r_silesia.pt`): trained clean (step 19,900, road CE 1.003, KL in band). W1' retrained base 0.7790 vs zero-shot 0.7817 (**−0.27pp, FAIL**); GT@30s (primary rate) hybrid 0.7360 vs nk 0.7709 (**−3.49pp, FAIL**); GT@60s hybrid 0.8506 vs nk 0.7622 (+8.84pp, pass, but does not offset the primary-rate failure per the pre-registered pass condition).
  - **Verdict: speed-filtering alone does not rescue per-city retraining on public traces. Zero-shot Porto-WM hybrid stands as the shipped OOD config; do not iterate further on public traces.** Open confound (encoder frozen/zero-shot during retrain, not yet isolated) triggers one bounded follow-up (encoder-unfrozen filtered retrain) before any final call on "public traces are too weak" vs "frozen-encoder mismatch." Raw: `.worktrees/research2/eval_gt_silesia_retrained.txt`, `eval_w1_silesia.txt` (iter 1); `eval_w1_silesia_iter2.txt`, `eval_gt_silesia_iter2_r{30,60}.txt` (iter 2). Full analysis: `run2_open_checks_resolution.md` Part A/Check-2, Part B/R3.
- **P1 data-scaling ablation (2026-07-14, gated probe from §5, DECISIVE PASS).** Same 2.6M model/recipe, batch 256, lr 4e-4, epoch-matched step counts (400k: 10k steps ≈6.4 epochs; 800k: 20k steps ≈6.4 epochs — same steps/traj ratio, not a step-count confound), evaluated via `eval_research2.py` on the standard held-out 500-traj split: **400k match@1 = 0.7772, 800k match@1 = 0.7965 → +1.93pp per doubling**, ~2x the pre-registered ≥+1pp/doubling data-bound bar. 200k-control (6k steps ≈7.68 epochs, slightly higher epoch count) evaluated 2026-07-14: **match@1 = 0.7559**. Full ladder now closed and monotonic: 200k→400k **+2.13pp/doubling**, 400k→800k **+1.93pp/doubling** — both clear the ≥+1pp/doubling bar independently, no flattening. Note 200k trained *more* epochs than 400k/800k (7.68 vs 6.4) yet still scored lowest, ruling out "200k just undertrained" as a confound — the gap is a real data effect, not a training-budget artifact. **This refutes the capacity diagnosis's prior "data-matched, not data-starved" assumption (§4 pt.3, was speculative Chinchilla-arithmetic, never tested) — data headroom is real and the current highest-leverage lever.** Notebooks: `.worktrees/data_scaling/notebooks_p1_{200k,400k,800k}/`.

## 4. Capacity verdict (2026-07-11 diagnosis)

**"Architecture too small" is NOT the primary bottleneck.** Evidence-ranked bottlenecks:
1. ~~**LR-schedule confound**~~ **— REFUTED by experiment (P0, 2026-07-12).** Was ranked #1 (final ~20k steps ran at LR ≤1e-6→1.7e-10; plateau feared uninformative). P0 tested it: resume run2xl, flat LR 3e-5, +10.3k steps → match@1 0.7677 vs baseline 0.7692 (**−0.15pp**), road CE not lowered. The plateau is **genuine convergence, not a dead-LR artifact**; the "free win from LR re-warming" hope is dead. (The +7.3pp historical gain was real but from the Run-1→Run-2 curriculum-bug fix, a different lever.) Bottleneck is now signal/data or capacity — P1/P2 the live probes.
2. **Label-free signal ceiling** — road head trains on geometry-derived soft pseudo-labels (CE floor = target entropy, never measured); hybrid adds only +2.1pp over geometry; prediction metrics flat from step 5k under 12× more training.
3. ~~**Unused data headroom**~~ **— CONFIRMED REAL by experiment (P1, 2026-07-14), the "data-matched" assumption below was wrong.** Was ranked #3 on untested Chinchilla-direction arithmetic. P1 tested it: 400k→800k match@1 +1.93pp/doubling, ~2x the bar. 800k of 1.7M Porto trajs used — headroom remains to the full pool. **Now the top-ranked, evidence-backed bottleneck** — see §5 for the next run.
4. **Latent width (the one supported narrow variant)** — 16×16 latent is a 4× down-scale of DreamerV3's universal 32×32 default; caps fine-GPS decode (cl_gps ~0.54km); but KL uses only ~22% of latent capacity, so road-task width is not saturated. DreamerV3 scales monotonically 12M→400M and our 2.6M is below their smallest config — bigger-WM prior can't be ruled out, hence probes.
5. Raw parameter count — least evidenced; no overfitting anywhere (nothing degrades across 12 ckpts).

## 5. Next steps (priority order)

**Product roadmap** (`practical_roadmap.md`): ② OOD test **DONE 2026-07-11** (fails — see §3; per-city retraining now first-class) → ① package **DONE 2026-07-11**: `.worktrees/research2/matcher.py` — `Matcher(city).match_offline/match_online/predict` on raw (lat, lon, t), auto mode switch (trained city → hybrid/WM, unseen → NK); smoke `test_matcher.py` all bars met (porto hybrid 0.8672≥0.83, online 0.8110≥0.70, predict connected, tdrive NK 0.8544≥0.75) → ④ scripted per-city retraining recipe (elevated by OOD result) → ③ latency/footprint benchmark → ⑤ optional bounded actor attempt.

**Training-side gated probes** (`run2_capacity_diagnosis_and_scaling_review.md` Part III; each needs a written success bar before launch; Kaggle runs need user go-ahead — note: this rule was superseded 2026-07-12, Kaggle now needs no permission, see §7):
- ~~**P0**~~ LR-tail probe — **DONE 2026-07-12, REFUTED** (see §4 pt.1): flat-LR resume moved match@1 by −0.15pp, not the ≥+1pp bar. Plateau is genuine convergence.
- **P0b** instrument — **soft-target-entropy half IMPLEMENTED 2026-07-15** in `.worktrees/research2/training/stage2r.py` (`wm_losses` no_grad stats, prompted by `research/improvement_experiment.md` review — the only adopted item from that doc; its KL-floor claim was verified FALSE, code is per-group DreamerV3 free bits): now logs `tgt_ent` (soft-target entropy = road-CE floor), `road_ex`/`prior_road_ex` (excess CE = CE − H(ỹ) = KL(target‖pred), the true distance-to-floor), `kl_act` (fraction of latent groups above the 1-nat free-bits floor), `post_ent`/`prior_ent` (latent entropies). Zero new params/RNG → resume-compatible with the P1-cont session-1 ckpt (step ~8,950), gradients bit-identical (ladder comparability preserved). **DEPLOYED 2026-07-15: new `alphaevolve-code-research2` dataset version pushed (status ready; pre-upload diff vs deployed = exactly the instrumentation hunks, OOM fix intact) — next P1-cont resume session picks it up automatically; no kernel launched (GPU quota exhausted).** Held-out-loss half still open. **Training-progress read from now on: gate on per-ckpt eval metrics (match@1 vs step) as always; loss-side use `road_ex`/`prior_ex` (floor = 0 by construction), never `total` (has the 9.6-nat KL floor + unknown teacher entropy baked in).
- ~~**P1**~~ data ablation — **DONE 2026-07-14, DECISIVE PASS, full ladder closed** (see §3, §4 pt.3): 200k=0.7559, 400k=0.7772, 800k=0.7965 → +2.13pp then +1.93pp/doubling, both ~2x the bar, monotonic, no flattening.
- **P1-cont (LAUNCHED 2026-07-14, RUNNING on Kaggle T4 — see sub-bullet for the OOM fix; recommended via openevolve-researcher literature check):** continue the data ladder to the full available pool (~1.6–1.7M Porto trajs, the next doubling of 800k) — same 2.6M model/recipe, batch 256, lr 4e-4, epoch-matched (~6.4 epochs → ~40,000 steps), `--resume`-chained across Kaggle T4 sessions same as the 800k run. **Bar: match@1 slope 800k→1.6M ≥+1.0pp/doubling** (same bar as P1, for consistency). If met → data axis still dominant, prioritize data-side work (dedup/quality filtering, augmentation) over architecture next. If not met → that flattening is the literature-grounded trigger to run P2 next (data-scaling-law literature — [arXiv:2504.04338](https://arxiv.org/html/2504.04338v1), UniTraj, MoveGPT — says capacity probes are diagnostic *after* the data curve bends, not before). Evaluate via `eval_research2.py`, same held-out 500-traj split.
  - **Precompute detour, 2026-07-14:** full pool = 1,658,437 trajs (confirmed whole GPS file, not a subset). One-shot CPU precompute kernel OOM'd (`DeadKernelError`) — root cause: resident `cand` arrays (7 fields × `n_fixes × k=10`) scale with full-file `n_fixes`, independent of the STRtree query-phase chunking already in `_precompute_candidates`; at full pool that's ~32.9GB alone vs the ~30GB Kaggle CPU ceiling (math cross-checked against the 800k run's known ~15-19GB peak — matches). Fix: added `skip_trajs` offset param to `load_source_df` (`dataset/trajectories.py`), split precompute into 2 chunks — reuse existing `porto_n800000_r50_k10.npz` (chunk 1) + compute only the 800k→1658437 delta (chunk 2) — then merge via direct-to-zip incremental write (not a RAM-resident merged dict, which would've re-hit ~36GB) into `porto_n1658437_r50_k10.npz`. Verified `skip_trajs` locally before spending Kaggle CPU-hours. Precompute kernel v6 relaunched 2026-07-14; GPU training (`notebooks_p1_1658k/`) gated on this cache landing on `alphaevolve-precompute-cache`.
  - **GPU-training OOM (2026-07-14, distinct from the precompute OOM above — FIXED, run relaunched).** Cache `porto_n1658437_r50_k10.npz` landed (n_rows=82,351,535). GPU kernel `notebooks_p1_1658k/p1_1658k.ipynb` (id `alphaevolve-p1-cont-1658k-t4`) then died SIGKILL 9 (=Linux OOM-killer, no traceback) on the FIRST two attempts. Root cause: `_load_or_build_cache` matched `int(z["n_rows"]) == n`; cache had 82,351,535 rows but the training df has 82,351,446 (89-row parquet-tail diff) → cache MISS → `_precompute_candidates` ran `np.full((n,k))` for all 7 fields ≈33GB + df 14.16GB ≈47GB > ~29GB Kaggle RAM → OOM. The np.full is *before* the first `[precompute]` print, so a MISS dies silently (log-identical to a HIT-path OOM). Fix, two changes in `.worktrees/data-preprocess/dataset/trajectories.py` (redeployed to `alphaevolve-code-research2`, **uncommitted** as of this session): (1) cache match `==`→`>=` (`n_rows >= n` prefix-reuse — safe because `load_source_df` reads `part-000.parquet` strictly top-down, so a df of n fixes is an exact row-prefix of any larger same-parquet cache and `__getitem__` only indexes [0,n)); (2) new `_memmap_cache(z,n)` writes each field to `$CAND_MMAP_DIR/candmm_n{n}_k{k}/{field}.npy` (default `/kaggle/temp`, ext4 real disk) and returns `np.load(..., mmap_mode='r')` — resident RAM ~0, peak ~21GB, under the ceiling. (npz can't mmap directly; `mmap_mode='r'` works on `.npy` only.) **v4 RUNNING on Kaggle T4** past the ~1min death point, `--max-hours 6.5 --ckpt-every 1000`, resume-chained via `stage2r_porto_p1_1658k.pt` (< the 7hr quota → saves a partial checkpoint, continues next cycle; 6.5hr ≠ full ~41,500-step schedule). **Kaggle notebooks don't stream logs** — step-level match@1 only visible at commit (~6.5hr) or on ERROR; RUNNING-past-death-point is the only live OOM-cleared signal. See memory `project_kaggle_oom_cache_fix.md`.
  - **Session-1 partial checkpoint eval (2026-07-14, INTERIM — not a final slope datapoint).** Kernel `alphaevolve-p1-cont-1658k-t4` completed session 1 (fresh, `resume: fresh (session 1)` — no prior ckpt existed): reached step ~8,950 before the 6.5hr cutoff = **8,950/41,500 steps (21.6% of the planned schedule), ~1.38 epochs** vs the 800k baseline's fully-trained ~6.4 epochs. Pulled the final `stage2r_porto.pt` and evaled once on the standard held-out 500-traj split via `eval_research2.py`: **match@1 = 0.7923** (kl 11.2 / ent 25.6 / cl_gps 0.518km — all consistent with an under-converged model). Raw slope vs 800k=0.7965 is **−0.42pp** (would FAIL the ≥+1pp bar) **but the comparison is undertrained-vs-converged and therefore not decisive** — the 1.66M model has seen ~1/5 the training of 800k yet already lands within 0.42pp of fully-trained 800k, so the run is plausibly on track to clear the bar once trained out. **Real read needs ~4 more resume sessions to reach 41,500 steps; P1-cont status stays RUNNING, no verdict recorded.**
  - **Resume chain seeded (2026-07-16, Kaggle GPU quota exhausted).** `alphaevolve-ckpt` dataset v2 pushed (status ready) with the session-1 final ckpt as `stage2r_porto_p1_1658k.pt` (torch-verified step 8,971, full opt+sched state; stage0/1 + run2xl kept so nothing dropped) — the Kaggle kernel's `RESUME_SRC` now resolves, so the next session resumes automatically once weekly quota resets. Platform-neutral: any box that can download the 5 datasets (~5.9GB: gps parquet 1.85GB + only the n1658437 cache 3.98GB) and push the advanced ckpt back can take a session.
  - **Session 2 (2026-07-18, quota reset — COMPLETE, interim).** Kernel v5 resumed cleanly from step 8,971 → **16,241** (+7,270 steps, 6.63h wall, ~3.05 s/step — slower than session 1's 2.6 because no short-seq curriculum phase this time; 39% of the 41,500-step schedule, ~2.5 epochs). P0b instrumentation live in-session: final line `road_ex 0.2608 / prior_ex 0.4392 / tgt_ent 1.0366 / kl_act 0.065 / post_ent 14.88 < prior_ent 25.37` — posterior encoding, no collapse. Held-out eval (`eval_research2.py`, standard 500-traj split): **match@1 = 0.8016** — already **+0.51pp above fully-trained 800k (0.7965) at 39% of training** (session 1 was −0.42pp below); cl_gps 0.457km (was 0.518), kl 11.2, prediction hit@1 0.666 (above Run-3 RSSM best 0.648). Bar for the leg (≥+1.05pp over 800k = 0.8070) not yet met but trajectory strongly favorable — **still interim, no slope verdict until trained out (~3.5 more sessions)**. Chain reseeded: `alphaevolve-ckpt` v3 pushed (status ready, step-16,241 ckpt as `stage2r_porto_p1_1658k.pt`, other 3 files kept) — next session auto-resumes. Raw: `.worktrees/research2/eval_p1cont_s2_step16241.txt`. **No further session launched — awaiting explicit go-ahead.**
  - **Session 3 (2026-07-19 — COMPLETE, bar cleared, still interim).** Kernel v6 resumed 16,241 → **23,540** (+7,299 steps, 6.5h train cap hit, ~3.2 s/step; 57% of the 41,500-step schedule, ~3.6 epochs cumulative). In-session health (step 23,500): `road_ex 0.2433 / prior_ex 0.4183 / tgt_ent 1.0048 / kl_act 0.064 / post_ent 15.06 < prior_ent 25.46` — posterior encoding, no collapse, road_ex still shrinking (0.2608 → 0.2433). Held-out eval (same 500-traj split): **match@1 = 0.8100 — leg bar (≥0.8070 = +1.05pp over 800k) CLEARED at 57% of training**, +1.35pp over fully-trained 800k; prediction hit@1 0.680 (new best, was 0.666; Run-3 RSSM 0.648); cl_gps 0.481km (slight regress from 0.457, watch); kl 11.0 healthy. Ladder now: 200k 0.7559 → 400k 0.7772 → 800k 0.7965 → 1658k **0.8100@57%** — final slope verdict still at train-out (~2.5 sessions left, number may rise further). Chain reseeded: `alphaevolve-ckpt` v4 pushed (ready, step-23,540 as `stage2r_porto_p1_1658k.pt`). Kaggle CLI output pull flaked (ConnectionResetError on the 5.9GB cache npz that landed in `/kaggle/working`); weights hand-downloaded, archived at `Downloads/results/ckpt/` (hash-verified vs Kaggle web's `.zip`-renamed copies — same bytes). Raw: `.worktrees/research2/eval_p1cont_s3_step23540.txt`. Within-session ckpt ladder (all 8, same split): 16,241 0.8016 / 17k 0.8036 / 18k 0.7992 / 19k 0.8034 / 20k 0.7945 / 21k 0.8003 / 22k 0.8095 / 23k 0.8001 / final 23,540 **0.8100** — final is best, ±0.8pp step-to-step noise, no overfit signature (raw: `data_scaling/eval_s3_ladder_{a,b}.txt`). Weights archive: `Downloads/results/ckpt/`.
  - **Session 4 (2026-07-19 — COMPLETE, interim).** Kernel v7 resumed 23,540 → **30,798** (+7,258 steps; 74% of schedule, ~4.8 epochs cumulative). In-session health (step 28,050 spot-check): kl 9.68, post_ent 14.94 < prior_ent 25.41, road_ex 0.258, cl_gps_z 0.347 — no collapse, lr 9.7e-05 deep in cosine decay. Full ckpt ladder (same 500-traj split): 24k 0.8010 / 25k 0.8037 / 26k **0.8074** / 27k 0.8045 / 28k 0.8060 / 29k 0.8069 / 30k 0.8034 / final 30,798 **0.8054**. Read: session band 0.8010–0.8074 (mean ≈0.8048) vs session-3 band mean ≈0.8025 — still drifting up but decelerating; s3-final's 0.8100 was a high-side noise draw, not regression. Bar 0.8070: band top touches it, mean below — **train-out decides (~1.5 sessions left, lr anneal usually lifts the tail)**. No overfit signature. hit@1 best this session 0.668 (26k). Chain reseeded: `alphaevolve-ckpt` v5 (ready, step-30,798). Notebook fixed for next push: cell 5 now `rmtree`s the 5.9GB candidate cache from `/kaggle/working` post-train so kernel output = weights only (CLI pulls won't choke again). Weights archive: `Downloads/results_1/ckpt/`. Raw: `data_scaling/eval_s4_ladder_{a,b,c}.txt`. **No session 5 launched — awaiting explicit go-ahead.**
  - **Session 5 (2026-07-20 — COMPLETE, interim).** Kernel v8 resumed 30,798 → **39,035** (+8,237 steps, ~2.8 s/step; **94% of schedule**, ~6.0 epochs cumulative). Cache fix confirmed live: user's kernel-output download held weights only — no 5.9GB `npz` (cell-5 `rmtree` worked). Full ckpt ladder (same 500-traj split; step39000 skipped — 35 steps from final = identical weights): 31k **0.8080** / 32k 0.8044 / 33k 0.8052 / 34k 0.8042 / 35k 0.8037 / 36k 0.8062 / 37k 0.8030 / 38k 0.8048 / final 39,035 **0.8060**. Read: band mean ≈0.8051 (9 draws) vs session-4 ≈0.8048, session-3 ≈0.8025 — **plateau** (+0.03pp s4→s5 vs +0.23pp s3→s4; the up-drift has flattened). Best draw 31k 0.8080 clears bar 0.8070 but that's a high-side noise draw — mean sits under. +0.86pp over 800k-full 0.7965; bar wants +1.05pp (0.8070), gap 0.19pp at 94%. No overfit signature (band flat, not falling). **Train-out (~2,465 steps → 41,500) decides**: deceleration makes a 0.19pp mean gain in one short session unlikely; lr-anneal tail may still lift the final ckpt. Honest read — likely photo-finish just under bar → 2.6M capacity saturation near 0.805, which is itself a clean result that **motivates P2** (latent 16×16→32×32). Chain reseeded: `alphaevolve-ckpt` v6 (ready, step-39,035). Weights archive: `data_scaling/ckpt/` (user-placed this session). Raw: `data_scaling/eval_s5_ladder_{a,b,c}.txt`. **No session 6 launched — awaiting explicit go-ahead** (session 6 = final ~2,465-step train-out to 41,500 → slope verdict vs bar 0.8070).
  - **P1-cont leg CALLED (2026-07-20 — user decision + openevolve-researcher verdict; session-6 train-out SKIPPED).** Rationale: decelerating session-mean (+0.23pp s3→s4, +0.03pp s4→s5) extrapolates ~0.01–0.03pp over the final 6% of the schedule — 6–15× short of the 0.19pp gap to bar 0.8070; the late-plateau rescue mechanism (LR re-warm) was already falsified (P0: −0.15pp). **Leg result: bar MISSED** — plateau ≈0.805, band mean 0.8051 = +0.86pp over 800k-full vs +1.05pp required. Independent kicker: **1.66M = the full Porto pool**, so no further data doubling exists — a bar-clear would have been moot for the 'prioritize data-side next' branch anyway. Data curve bent **and** pool exhausted → **triggers P2** per §5 pre-registered logic. **Next action = P2** (latent 16×16→32×32, `models/world_model2.py:88` `groups`/`classes` — 2-value change, reuses the 1.66M candidate cache). Cheap non-GPU pre-step before trusting any P2 pass: recompute a fresh **1.66M/16×16 hybrid-Viterbi anchor** (the on-file 0.8655 P2 bar is a stale 200k-data baseline) via `matcher.py` on `data_scaling/ckpt/` step-39,035 — P2 must beat that fresh anchor by > pipeline noise (~0.15–0.3pp) to count as a capacity win,  in addition to clearing 0.8655/0.40km. **Anchor DONE 2026-07-20: 0.8642** (tol_hit@1, n=100 held-out from offset 200k, sd 0.109 → s.e. ~1.1pp) — statistically **== the stale 0.8655 bar** (−0.13pp, ≪1 s.e.) and matches the pre-data-scaling `matcher.py` 0.8655 / smoke 0.8672. **Key finding: the hybrid offline metric is geometry-saturated** — 8× data moved the road-head match@1 ladder (0.7559→0.805) but did NOT move hybrid (still ~0.864), because hybrid Viterbi is geometry-dominated (+2.1pp over pure NK). **Implication: judge P2 primarily on the `cl_gps ≤0.40km` bar** (fine-GPS decode = the latent-specific capability, currently stuck ~0.48km); the hybrid >0.8655 bar is near-unmovable and would under-credit a real capacity win. Raw: `data_scaling/hybrid_anchor_1658k_16x16.txt`. Watch `kl_act` through P2 (if extra groups stay low-util, the lever is added classes/resolution, which is what targets the stuck cl_gps ~0.45–0.52km floor). Citation fix: §5's `arXiv:2504.04338` = Naumann et al. *Data Scaling Laws for E2E AD* (CVPR'25 wksp), not the Pearce entry — real, relevant, uncatalogued; its closed-loop metric is rescued by neither data nor capacity alone but only both together → reinforces P2 needing its hard falsification bar. **No P2 session launched — awaiting explicit go-ahead.**
- **P2** latent 16×16→32×32 (DreamerV3 default) — tests the one supported capacity hypothesis; bar: hybrid >0.8655 or cl_gps ≤0.40km. **Sequenced after P1-cont**, not combined with it (single-variable-per-run design; combining now would break the clean 400k/800k read).
  - **P2 session 1 LAUNCHED 2026-07-25 (user 'resume', RUNNING on Kaggle T4 `alphaevolve-p2-32x32-t4`).** Latent 16x16->32x32 via NEW `--stoch-groups/--stoch-classes` flags in `training/stage2r.py` (rssm+heads built with them; ckpt `meta` now records the width). **NOT the pre-reg's '2-value `world_model2.py` edit'** -- that undercounted: `STOCH_DIM=256` (world_model.py:27) is the default width for every head (RoadScorer/RewardScorer/HeadsLight/Critic) AND RSSM2's literal `groups=16` (world_model2.py:88) shadows the constant, so a naive default-bump would desync heads vs RSSM (256 vs 1024 crash) AND break every existing 256-wide `load_state_dict` through the SHARED `alphaevolve-code-research2` dataset (deployed matcher, whole data-leg ladder). A flag defaulting to 16/16 is byte-identical for existing runs, opt-in for P2 -- the correct isolation (design decision, CLAUDE.md §7). rssm+heads 2.60M->**4.77M** params (local 32x32 shape-check PASS: full prior/post/heads/road/reward/KL path wires clean, free-bits floor auto-scales 16->32 groups; note cell-3 `--smoke` only exercises 16x16). Recipe otherwise IDENTICAL to P1-cont (batch 256, lr 4e-4 cosine, 41,500 steps ~6.4 epochs, curriculum-step 4000) = clean single-variable capacity ablation vs 1.66M/16x16. Fresh init (categorical shapes change -> no warm-start); resume-chained on OWN file `stage2r_porto_p2_32x32.pt` (session-1 fresh since it doesn't exist yet); `--max-hours 10.0` (user cap 10.5h; buffer for save+cache-drop+snapshot). Code dataset re-versioned (status ready, flag shipped, OOM/P0b fixes intact). Notebook `data_scaling/notebooks_p2_32x32/`. **Judge on cl_gps <=0.40km** (2026-07-20 anchor: hybrid geometry-saturated), watch `kl_act` (low-util extra groups => capacity isn't the lever). On completion: pull weights -> full ckpt-ladder eval -> reseed `alphaevolve-ckpt` as the p2 file. **Session 2 awaits explicit 'resume'.**
  - **P2 mid-run diagnosis (2026-07-26, session 2 RUNNING, step ~16,650/41,500 = 40%).** `kl_act` (fraction of latent groups above the 1-nat free-bits floor) = 0.010-0.036 throughout — only **~1 of 32 groups** ever carries gradient. Code-verified not a bug: free-bits (1 nat/group), KL-balance (0.5 dyn/0.1 rep), 1% unimix all match DreamerV3 spec exactly (`world_model.py` `_mixed_probs`/`kl_categorical_pergroup`). Cross-checked against the 16×16 P1-cont run's own P0b logs (sessions 2-3): `kl_act` there was 0.064-0.065 — also ~1 of 16 groups active — read at the time as width not increasing *active-group count*. Literature check (`openevolve-researcher`): dead-code/codebook revival (Jukebox, SQ-VAE, FSQ) targets shared nearest-neighbor codebooks, no clean port to independent per-group softmax categoricals; adaptive free-bits (delta-VAE, SQ-VAE self-annealing) would need first-principles re-derivation. Cheapest lever identified: pseudo-label sharpening (ASTRA-style). New sources cached: `literature_papers/{fsq_finite_scalar_quantization,delta_vae_preventing_posterior_collapse,sqvae_self_annealed_stochastic_quantization,jukebox_codebook_random_restarts,astra_self_training_weak_supervision}.json`. **This diagnosis's conclusion ("don't launch P3") was PREMATURE and is corrected by the actual session-2 eval below — kl_act (active-group count) does not capture where the capacity win comes from (likely the h-pathway / within-group class resolution below the 1-nat threshold, not raw group activity).**
  - **P2 session 2 result (2026-07-26, COMPLETE, interim — hit `--max-hours 10.0` at step 24,231/41,500 = 58%).** Full ckpt-ladder eval (`eval_research2.py`, same held-out 500-traj split, road-head decode, GPU): **match@1 band 0.8297-0.8433 across all 12 ckpts (13k-24k), final 0.8371** — a decisive, tight (±0.7pp), already-flat band **far above every prior road-head-only number**: Run-2-XL/200k/16×16 0.7684, P1-cont/1.66M/16×16 best-interim ~0.8100. cl_gps (primary bar, ≤0.40km): 0.3568-0.4041 band, **final 0.3603 — PASS**, only 1 of 13 ckpts (step16000, 0.4041) marginally misses. kl 22.8-23.6 (raw, ≈0.71-0.74/group avg over 32 groups) — within the healthy 0.4-0.9/group band, no collapse. **Verdict: P2 is a real, decisive capacity win at only 58% of its schedule — width DID help despite low kl_act, correcting the mid-run diagnosis above.** Bar (cl_gps ≤0.40km) cleared; match@1 gain over 16×16-at-same-data (~+2.5-3.3pp over P1-cont's ~0.805-0.810 ceiling) is new evidence *for* capacity, not against it — **P3 should NOT be ruled out on this basis; re-open it as a live option, not downgraded.** Hybrid-Viterbi (secondary bar, >0.8655/anchor 0.8642) not yet run — road-head-only result already clears the primary bar decisively, offline hybrid check deferred (archived script, would need re-wiring against the 32×32 ckpt) unless a final publishable number is needed. Chain reseeded 2026-07-26: `alphaevolve-ckpt` new version pushed with `stage2r_porto_p2_32x32.pt` = step-24,231 final. **No session 3 launched — flat 13k-24k band suggests possible near-ceiling for this window, but LR is still mid-cosine-decay (P1-cont's own late-schedule anneal lifted the tail in prior ladders) — training out the remaining 42% is the honest next step before a final verdict, awaiting go-ahead.** Raw: local eval run 2026-07-26, `.worktrees/research2/eval_research2.py` against downloaded `p2_32x32_final/ckpt/`.
  - **Session-3 decision (2026-07-26, user call, DATA-CONFIRMED).** User flagged: session-2 ckpt ladder not improving + LR decaying fast + only 9.5h weekly Kaggle GPU quota left. Verified against the log: LR 3.17e-04(step12850)→1.51e-04(step24200), already halved; `road_ex`/`cl_gps_z_km` flat-noisy over the same span. Budget math: session 2 ran ~3.2s/step; 9.5h buys ~10,700 more steps → best case reaches ~step 34,900 (84%), CANNOT reach 41,500 this week regardless. **Decision: do not spend session 3 on more P2 training — call the leg at step 24,231/58%** (same precedent as P1-cont's session-6 skip). Redirect the saved GPU budget to OOD-testing this WM on T-Drive (owed, never done for the wider WM) instead of chasing a flat curve.
  - **Hybrid+topo confirm (2026-07-26, local GPU inference, zero Kaggle quota spent, `matcher.py` + `test_matcher.py`'s `tol_hit` methodology, full 500-traj held-out split).** Converts the road-head-only 0.8371 into the actual deployed-pipeline numbers: **offline hybrid Viterbi = 0.8679** (new project best, was 0.8655) — **online hyb+topo = 0.8287** (new project best, was 0.8122, **+1.65pp**, the harder/production-relevant metric). Sanity anchors held: nk-only offline 0.8426 (≈0.8447 expected), bare-WM online 0.8399 (≈0.8371 from `eval_research2.py`, cross-harness consistent). **Verdict: the P2 capacity win is real and survives contact with the actual shipped pipeline, not just the road-head ablation — at only 58% training.**
  - **Promoted to default (2026-07-26, user decision).** `.worktrees/research2/ckpt/stage2r_porto.pt` (the file `Matcher(city, ckpt="final")` resolves to) swapped: old 16×16 Run-2-XL backed up to `stage2r_porto_run2xl_16x16_final.pt` (preserved, not deleted), new P2 32×32 step-24,231 ckpt copied in as the default. Re-ran `test_matcher.py` smoke (n=25) against it: offline hybrid 0.8742, online hyb+topo 0.8174, `predict` connected, tdrive nk-fallback 0.8544 — all clear the existing pre-registered floors (0.83/0.70/0.75) with margin, no bar changes needed (floors are regression guards, not exact-match targets). **`matcher.py`'s live default is now the P2 32×32 checkpoint** — Porto production numbers are 0.8679 offline / 0.8287 online (500-traj), until session 3 (if ever run) supersedes it.
- **P3** joint scale-up (~DreamerV3-XS class) — **re-opened 2026-07-26.** The mid-run kl_act diagnosis's "downgrade P3" call was premature; the actual session-2 eval shows a decisive capacity win (see above), so width is back on the table pending P2's full train-out. **P4** signal-side work (contrastive auxiliaries, cross-city data, pseudo-label sharpening) stays queued as the next lever if the data/capacity axis ever genuinely flattens — not yet triggered.
- **Dual-agent capacity/RL research (2026-07-26, two parallel `openevolve-researcher` agents, literature-first, 53-file catalogue + new fetches).** Agent A (capacity/algo): **resolved the kl_act puzzle more rigorously** — raw total KL grew *super-proportionally* to latent width, not just proportionally: 16×16 ≈9.7 nats/16 groups = 0.6 nats/group → 32×32 22.8-23.6 nats/32 groups = 0.71-0.74 nats/group — that's +20-25% per-group density stacked on top of 2× more slots (total KL +139% vs 2× the group count), which argues for genuine added information, not pure dilution/superposition. kl_act's binary 1-nat-threshold is a known-coarse VAE diagnostic (cf. Burda/Grosse/Salakhutdinov IWAE "active units," ICLR'16) — this refutes the mid-run "width didn't really help" reading a second, more quantitative time. DreamerV3's own Table B.1 (verified) fixes the latent at 32×32 across ALL five published sizes (XS-XL) — no precedent to scale latent width further. This project's GRU (h=512) already matches DreamerV3-"S" tier but the MLP heads (hidden=256, 1 layer) sit at "XS" tier — a real, checkable scale mismatch. **Top actionable pick: widen heads to S-tier (512-hidden/2-layer) AFTER P2 trains out**, single-variable vs the proven 32×32 config, ~4-5 Kaggle sessions, judge on cl_gps/match@1 only (hybrid stays geometry-saturated per the 2026-07-20 finding). Transformer/Mamba world-model rewrite (STORM/IRIS/Drama) evaluated and **parked** — no literature precedent on label-free/pseudo-label-CE POMDP road-matching tasks, and it would break the project's single-variable controlled-comparison lineage. Stacked-GRU/attention-hybrid and SiLU→GELU swap: **not worth a session** (no DreamerV3-lineage precedent for GRU depth; SiLU vs GELU shows no task-specific edge in general literature, DreamerV3's SiLU+LayerNorm choice already deliberate). Agent B (RL/policy): **actor track reaffirmed CLOSED**, on firmer ground than the in-project probe alone — the RLHF reward-overoptimization literature (Gao/Schulman/Hilton reward-overoptimization scaling laws ICML'23, "Catastrophic Goodhart" NeurIPS'24, Coste et al. reward ensembles, WARM) shows this project's own step-0-is-peak/monotonic-decline probe is the *degenerate boundary case* in that literature's own framework — where the reference policy already sits at the hill's peak, every standard fix (KL/trust-region, reward ensembling, iterative reward refitting) is mathematically or empirically capped at reproducing the already-deployed no-RL baseline, never beating it. No surveyed fix clears that ceiling; reopening needs a genuine new multi-step-sequential-decision product need (none currently exists — one-shot decode already wins outright), consistent with this project's own practical/product-metrics charter (§1), not a pillar-completeness argument. **Sequencing decision, corrected 2026-07-26 (session-3 train-out was already called off this same session — redirecting to it here was an inconsistency, fixed): (1) T-Drive OOD test of the P2 32×32 ckpt @58% (owed since promotion, cheap, inference-only) → (2) decide on the head-widen experiment using the already-flat 58% ckpt-ladder band (0.8297-0.8433, ±0.7pp across 12 ckpts) as the ceiling signal — flat-at-58% is itself sufficient evidence without spending the vetoed session-3 budget to reach 100%. RL actor: no further work absent a new product requirement.**
- **T-Drive OOD test of P2 32×32 (2026-07-26, local GPU, zero Kaggle quota — `eval_ood_tdrive{,_offline_viterbi}.py`, restored from `archive/` and rerun via `--only/--ckpt final` against the promoted P2 checkpoint).** Zero-shot Porto→T-Drive, 500 trajs, 14,165 fixes. Online: road-head match@1 **0.6567** (in-domain 0.8371, **−17.0pp**, ~78.5% retained — statistically the same OOD-drop pattern as the 16×16 result, which retained ~79.2%); cl_gps 0.508km (in-domain 0.360, degrades as expected off-city); kl 27.5 healthy, **no collapse OOD**. Offline: wm-argmax 0.6611, hybrid **0.7908** < nk-viterbi **0.7955** (nk-viterbi reproduces the recorded 0.7955 anchor exactly — sanity pass). **Verdict: same lesson as the 16×16 OOD test, confirmed again at higher capacity — the learned WM emission is city-specific and does not generalize; hybrid still loses to pure geometry OOD.** One mild, real improvement: the hybrid-vs-nk loss shrank from −0.8pp (16×16) to **−0.47pp** (32×32) — capacity narrows but does not close the OOD gap. **No city-agnostic claim stands; hybrid remains the trained-city (Porto) mode, NK remains the new-city fallback** — unchanged from the 2026-07-11 OOD verdict, now re-confirmed on the production checkpoint. Raw: `.worktrees/research2/eval_ood_tdrive_p2_32x32_{roadhead,offline_viterbi}.txt`.
- **Fork-ambiguity vs kl_act correlation diagnostic (2026-07-26, local CPU, `.worktrees/research2/diag_fork_kl_correlation.py`, run2xl 16×16 ckpt, held-out Porto split, same 500 trajs as `eval_research2.py`).** Tested the hypothesis that low kl_act reflects genuinely multimodal (multi-candidate, "fork") targets the discrete posterior can't/won't spend capacity on, by correlating per-(sample,timestep) active-group fraction against `_road_target_entropy` (the same Gaussian-softmax-over-candidates entropy the road-CE loss already trains against — high entropy = 2+ candidates comparably likely). n=20,129 valid+has-candidate timesteps, 15,241 (75.7%) above the ln(2) 2-way-tie threshold, 2,382 (11.8%) near-unambiguous. **Result: Pearson r = −0.046 (~zero, slightly negative). active_frac | fork = 0.071 vs | nonfork = 0.099 (fork LOWER, not higher; Cohen's d = −0.19, small).** Rules out "kl_act is low because true forks are rare" (75.7% of timesteps score as forks by this measure) — but does NOT cleanly confirm the multimodal-mismatch story either, since active capacity isn't concentrating on ambiguous points, it's flat-to-slightly-lower there. **Caveat, real: `_road_target_entropy` measures static geometric candidate-crowding (multiple candidates within the 10m-sigma kernel of a noisy GPS fix) at t, not true route-branching (does the vehicle face a real topological choice of successor road) — the null result may reflect this proxy's mismatch with "true multimodality" rather than refute the underlying idea.** Verdict: doesn't change the head-widen decision above; if the multimodal-target idea is pursued further, needs a graph-topology-based fork definition (successor-count/branch-entropy at the CURRENT road, not GPS-candidate crowding) before drawing conclusions.
- **kl_act probe: 32x8 latent shape (2026-07-26, Kaggle CPU, `alphaevolve-klact-probe-32x8-cpu`, 200k Porto default, steps=6000, reached step 4805/6000 at `--max-hours 8.5`; eval via new `.worktrees/research2/eval_klact_32x8_cpu.py`, standard 500-traj held-out split).** Same total categorical dim as 16×16 (32 groups × 8 classes = 256) but same GROUP COUNT as 32×32 — isolates groups-vs-classes-per-group as the variable in the ongoing kl_act investigation (Agent A's per-group-density finding, 2026-07-26). **Result: kl_act (converged, last ~300 steps) ≈ 0.030 — in the 32×32 family's range (0.010-0.036), not 16×16's (0.064-0.065)**, despite matching 16×16's total latent width. Supports "active-group COUNT is roughly constant (~1 group) and tracks group COUNT, not total capacity or classes-per-group" — consistent with the two fork-correlation nulls above (kl_act's ~7-8% active rate looks like a background rate decoupled from local signal, now decoupled from classes-per-group too). Eval (small-scale run, NOT directly comparable to the full 1.66M/41,500-step ladder): match@1 **0.7982**, cl_gps 0.4204km, hit@1/5/15 0.636/0.564/0.565 — already beats fully-trained Run-2-XL 16×16's match@1 (0.7684) at only 80% of a much shorter (6000-step) schedule on the same 200k-traj default, though the step/data budgets aren't matched so this isn't a controlled capacity comparison, just a directional data point. Per-group raw KL density ≈0.64/group (20.535 total/32 groups) — between 16×16's ~0.6 and 32×32's ~0.71-0.74, but again not scale-matched to those full-ladder numbers. **TPU counterpart (32×8, 8-core) probe failed again (2026-07-26) — user call: not investigating further right now.**
- **Follow-up: topology-based fork diagnostic (2026-07-26, local CPU, `.worktrees/research2/diag_topo_fork_kl_correlation.py`, same ckpt/split/n as above).** Replaced the geometric proxy with real graph topology: forward branching factor = total topological neighbors of the resolved (pseudo-label) current road segment minus the segment arrived from (via `SuccessorTable.contains`, reused from `training/rewards.py`, no new adjacency logic). Road graph: 154,144 segments, mean out_degree 1.77. Distribution: 0 forward options (dead-end) 7.1%, 1 (straight-through, no choice) 39.6%, 2 (real fork) 46.2%, 3+ 7.0% — genuine topological forks are 53.3% of timesteps, roughly balanced with non-forks (46.7%), not rare either. **Result: Pearson r = +0.048 (~zero, tiny positive this time). active_frac | fork = 0.080 vs | nonfork = 0.073 (Cohen's d = 0.054 — negligible).** Same overall kl_act (0.0767) as the geometric-proxy run — good cross-check, same population. **Combined verdict across both diagnostics: the null result is not a proxy artifact.** Two independent, opposite-sign-noise operationalizations of "fork" (GPS-candidate crowding: r=−0.046; graph-topology branching: r=+0.048) both land at ~zero correlation with per-timestep active-latent-group fraction. kl_act's ~7-8% active rate looks like a roughly constant background rate, decoupled from local multimodality by either measure — this does not support the video's "posterior collapses at multimodal points" mechanism as the explanation for *this* project's low kl_act (that mechanism would predict a clear positive correlation, which two different fork definitions both fail to show). Left open: `h` (the GRU recurrent state, not `z`) may already be carrying the fork-resolution signal from `road_keep_prob`-masked history, since the design intentionally keeps `z`'s job z-only-GPS-decode-focused — untested here, would need probing `h`'s content directly rather than `z`'s activity. Doesn't change the head-widen decision.
- **TPU kl_act probe (32×8, 8-core) v1 failed on a torch_xla API break, fixed and relaunched (2026-07-26).** First real run on `stage2r_tpu.py` (the from-scratch XLA rewrite, `.worktrees/TPU_kaggle`) errored at the device-diagnostic cell, before any training/quota spent: `AttributeError: module 'torch_xla.core.xla_model' has no attribute 'xrt_world_size'`. Root cause: Kaggle's actual installed torch_xla is **2.8.0**, not the 2.1 the bundled Kaggle skill docs describe (docs stale) — `xm.xrt_world_size()`/`xm.get_ordinal()` were removed torch_xla>=2.7, replaced by `torch_xla.runtime.world_size()`/`.global_ordinal()` (`import torch_xla.runtime as xr`). Also caught before it could bite: `xmp.spawn`'s `nprocs` has been restricted to `None`/`1` since 2.1 — it auto-spawns across every visible core now, an explicit count (what `--tpu-cores 8` was passing) would have failed the *training* cell next. Fixed in `stage2r_tpu.py` (`_is_master`, `world_size`/`rank` in `train()`, the `xmp.spawn` call site) and the notebook's diagnostic cell; local `--smoke` re-verified exact match post-fix (no-XLA code path unaffected, as expected). Re-pushed `alphaevolve-code-tpu` dataset v2 + kernel `alphaevolve-klact-probe-32x8-tpu` v2, QUEUED. **Lesson: bundled skill/reference docs for fast-moving library versions (torch_xla especially) can lag the actual installed version — verify API surface against the live environment, not cached docs, before writing XLA-version-sensitive code.**
  - **v2 ERROR, misdiagnosed (2026-07-26).** v2 got past the API break into the real run cell, then failed: `RuntimeError: Bad StatusOr access: UNKNOWN: TPU initialization failed: Invalid --2a886c8_slice_builder_worker_addresses specified. Expected 8 worker addresses, got 1.` First hypothesis (pytorch/xla#1587-shaped: the notebook's device-diagnostic cell touching `xm.xla_device()` in the main process locks the TPU as a single-worker client before `xmp.spawn` needs an 8-worker mesh) was plausible but **wrong** — isolated that cell into its own subprocess, pushed v3, **same identical error recurred**, proving the fix didn't touch the real cause.
  - **v3 ERROR, real root cause found (2026-07-26).** Pulled the full traceback this time instead of just the top-level message: the failure is *inside* `xmp.spawn` → `pjrt.spawn` → `pjrt.run_multiprocess`'s own `ProcessPoolExecutor`-based worker bootstrap (`torch_xla/_internal/pjrt.py: initialize_multiprocess` → `xm.get_xla_supported_devices()`), independent of anything the notebook itself touches first. Found the actual documented fix in PyTorch/XLA's own Kaggle example notebook (`pytorch/xla` repo, `contrib/kaggle/distributed-pytorch-xla-basics-with-pjrt.ipynb`): Kaggle kernels pre-set `TPU_PROCESS_ADDRESSES`/`CLOUD_TPU_TASK_ID` in the environment for the notebook's own single-process TPU access; left in place, `xmp.spawn`'s freshly-forked 8-worker mesh finds this stale 1-worker address config instead of computing its own — exactly "expected 8, got 1". The same official example also always passes `start_method="fork"` for interactive/papermill-executed notebook kernels. **Fix, this time in `stage2r_tpu.py` itself** (not the notebook): `os.environ.pop("TPU_PROCESS_ADDRESSES", None)` + `.pop("CLOUD_TPU_TASK_ID", None)` immediately before the `xmp.spawn` call, plus `start_method="fork"` added to that call. Re-verified `--smoke` locally (exact match, `kl_act 0.012` as before — note: must invoke as `python training/stage2r_tpu.py --smoke`, *not* `python -m training.stage2r_tpu --smoke`, from the `TPU_kaggle` worktree — the `-m` form makes `training` resolve to the local `TPU_kaggle/training` package first and shadows `research2/training`, a local-dev-only footgun, not a bug in the fix). Pushed code dataset v4 + kernel v4, QUEUED. **Lesson: verify a fix by re-reading the FULL traceback of the next failure before declaring success — a plausible-sounding, precedent-backed root cause (pytorch/xla#1587) was still the wrong one here; the identical error recurring after a "fix" is the tell to re-derive from the actual stack trace, not to assume a different rare trigger of the same known issue.**
- **L3 resolved: encoder-unfrozen Silesia retrain (2026-07-26, local GPU, `ckpt_car_unfrozen/`, --unfreeze-encoder, 5,500 speed-filtered trajs, stopped by its own --max-hours 6.0 cap at step 13,200).** Isolates the last open R3 confound from `run2_open_checks_resolution.md` (archived, unedited): was iteration 2's Silesia-retrain FAIL (W1' −0.27pp, GT@30s −3.49pp) caused by data contamination, or by the frozen Porto stage0 encoder never adapting to Silesia's road graph? Found and fixed a real eval-harness gap first: `Matcher._road_z()` always loads the frozen Porto encoder regardless of which WM ckpt is passed, so the stock eval scripts would have silently re-tested the frozen encoder under a new name. Patched `eval_w1_silesia.py` and `archive/eval_gt_kubicka_wm.py` with an opt-in override that loads `ckpt["road_enc"]` (present only when a ckpt was trained with `--unfreeze-encoder`) in place of the frozen encoder — backward-compatible, default path unchanged. **Result: W1 proxy (isolate-WM `base` mode, n=6,949, the metric built specifically to isolate the WM road head from NK) — unfrozen retrained 0.732-0.740 vs zero-shot 0.781-0.784, a ~-4.3 to -4.9pp gap, WORSE than iteration 2's frozen-encoder gap (-0.27pp).** GT@30s (n=659, hybrid/nk/online) came back numerically identical to iteration 2's archived frozen-encoder numbers (hybrid pooled 0.7360 both times) — flagged as a caveat, not trusted as corroborating signal: this metric is NK-dominated at this small n and may simply be saturated/insensitive to which Silesia WM is plugged in (confirmed hybrid IS ckpt-sensitive in general — zero-shot hybrid = 0.8877, clearly different), so it isn't proof of a harness bug, just weak evidence either way. **Verdict: unfreezing the encoder did not rescue the retrain — it made the cleanest isolate-WM signal worse, not better. This rules out "frozen encoder mismatch" as the cause and reinforces the original diagnosis: public Silesia traces are genuinely too weak/contaminated for label-free per-city retraining, not a fixable confound.** L3 closed, R3 resolved. Per the pre-registered stop rule (project_summary line 33 above): do not iterate further on public-trace Silesia retraining — the only remaining lever is cleaner data (Grab-Posisi-L request or real fleet data), otherwise redirect to the already-queued head-widen experiment.
- **Hybrid-cities + wider-head data prep, started (2026-07-26, user decision).** Direction: combine multiple clean (vehicle-only) cities for training instead of single-city Porto, alongside the queued head-widen experiment. Step 1, local data audit + prep:
  - **T-Drive is already fully prepared and unused for training** — processed parquet + OSM road graph (`data/osm/tdrive`, bbox 116.05-116.7E/39.7-40.2N, 172,777 segments) both exist from the OOD-eval work; it has never been included in an actual training run, only zero-shot eval. Cheapest immediate lever for a second training city — taxi-only by construction, no contamination risk.
  - **New: `geolife_car`** (`.worktrees/data-preprocess/preprocessing/parsers.py`, `iter_geolife_car` + `PARSERS` registry entry) — filters GeoLife (previously unused for training, on disk since before this project's Silesia work) down to the 69/182 users who carry ground-truth per-segment transportation-mode labels (`labels.txt`), keeping only "car"/"taxi"-tagged time windows. This is a strictly better recipe than Silesia iteration 2's speed-threshold heuristic (which let walker/rail speeds through and poisoned that retrain) — uses actual labels, not inferred speed. Result: 1,336 raw labeled car/taxi runs -> 2,207 clean segments, 718,788 points after standard clean/dedup/speed filtering. **84.8% of points fall inside the Beijing metro bbox** (the rest is scattered international travel by the same users, e.g. lon as far as -122 -- to be dropped/clipped, not graphed); 78.3% fall inside T-Drive's tighter existing bbox, meaning `geolife_car`'s Beijing-area segments can likely reuse the existing `tdrive` OSM graph directly (no new Overpass build needed) once clipped to that extent.
  - **Housekeeping note:** `preprocessing/preprocess.py`'s manifest.json write is NOT incremental — it overwrites with only the sources passed on that invocation. Running `--sources geolife_car` alone clobbered the historical geolife/tdrive/porto stats; manually restored/merged this time. Future preprocessing runs should pass all sources together (or manually re-merge) to avoid repeating this.
  - **Silesia_car explicitly excluded** from the hybrid-cities mix — closed dead end per the L3 result immediately above.
  - **Grab-Posisi** (SEA ride-hailing, car/motorcycle-mode metadata) remains the highest-quality known additional source but is gated behind a manual request form to Grab — needs the user's own action, not fetchable directly.
  - **`geolife_car` BBOX bug found+fixed same day:** `schema.py`'s `BBOX` dict (keyed by source name, used by `clean_trajectory` to drop out-of-city fixes) had no `"geolife_car"` entry, so the first run's bbox filter silently no-opped (`dict.get` returns `None` for a missing key) and let the ~15% international-travel noise through. Added `"geolife_car": (39.4, 41.2, 115.4, 117.6)` (same Beijing box as `geolife`/`tdrive`) and re-ran: **1,816 clean segments, 609,227 points, now fully Beijing-confined. 92.4% of points fall inside T-Drive's existing tighter OSM graph bbox (1,710/1,816 segments)** — confirms `geolife_car` can reuse `data/osm/tdrive` directly (the remaining 7.6% just fall outside the candidate index and match as unmatched, same as any out-of-graph fix already handled elsewhere in the pipeline). No new Overpass build needed.
  - **Web research done (task #19, background agent, completed) — findings corrected against direct verification, not taken at face value:**
    - Agent's top pick, **Thessaloniki (Greece) taxi-fleet GPS feed — downgraded on direct check.** Confirmed live and current (`curl`'d it, timestamps match today), but the feed has **no vehicle/trip ID field at all** (columns: timestamp, lon, lat, altitude, speed, orientation) — it's an anonymous fleet-position snapshot, not reconstructable per-vehicle trajectories, and there is no historical bulk archive (live-only, paged 500 records/request). Not usable as-is; would need sustained polling plus an unreliable nearest-position trajectory-reconstruction heuristic. Not worth pursuing over the alternatives below.
    - **WorldTrace (UniTraj's pretraining corpus)** — publicly downloadable (Hugging Face `OpenTrace/WorldTrace`, ODbL-1.0), but confirmed **no per-city/country subset download — 35GB all-or-nothing** for now (a streaming-sample audit via HF `datasets` may be possible, not yet tried). No vehicle/mode label or city field in the trajectory schema itself (filtered upstream only by OSM-uploader tags "indicating motorized movement") — same unverified-purity shape as the traces that poisoned Silesia. **Do not add to the training mix without an independent speed/road-snap purity audit first** — deferred, not started (real disk/bandwidth cost for a 35GB pull, flagging rather than pulling blind).
    - **Rome taxi (CRAWDAD roma/taxi) and San Francisco Cabspotting (CRAWDAD epfl/mobility) — user created the IEEE DataPort account themselves (assistant declined to, per the account-creation/password rule) and downloaded both (2026-07-26).** Processed into the pipeline same day: `iter_cabspotting`/`iter_romataxi` added to `preprocessing/parsers.py` (Cabspotting: per-cab `new_<id>.txt`, "lat lon occupied unix_ts", files newest-first — sorted ascending; Rome: single 21.8M-row `taxi_february.txt`, "id;timestamp+tz;POINT(lat lon)" — parsed via pandas, tz suffix stripped per the project's naive-local-clock convention). Both taxi-only by construction, no mode-filtering needed (unlike GeoLife). BBOX entries added to `schema.py` and `roadgraph/config.py`. **Result: `cabspotting` — 536 taxis, 137,349 clean segments, 11.05M points; `romataxi` — 316 taxis, 15,854 clean segments, 21.16M points.** OSM road-graph builds for both kicked off. One real bug hit+fixed: Rome's fractional-seconds field has a *variable* digit count (not always 6), so a fixed-width timestamp slice broke pandas' strptime on shorter rows — switched to splitting on the `+` tz-sign and parsing with `format="ISO8601"` instead of a fixed slice width. **OSM graphs built and sanity-checked (2026-07-26):** `cabspotting` (SF Bay Area) — 271,760 segments, 110,197 nodes, 292s; `romataxi` (Rome) — 245,896 segments, 113,443 nodes, 664s. `CandidateIndex` alignment check: a 20-point random sample from each city's processed GPS all matched a road segment within meters (0.3-25m); the very first 5 rows of `cabspotting` initially looked broken (no candidate within 50m) but that's the first cab's shift-start point sitting in what's evidently a depot/lot off any driveable OSM way — same as any normal unmatched fix, not a graph/data misalignment. **Five clean vehicle-only cities now fully prepped end-to-end (processed parquet + OSM graph, ready to train on): Porto, T-Drive, `geolife_car`, `cabspotting`, `romataxi`.**
    - **DiDi GAIA (Chengdu/Xi'an ride-hailing, ~3s sampling)** — high purity, gated behind a research-access application (unknown turnaround) — same as Grab-Posisi, needs the user's own action to file.
    - Deprioritized/skip: Shanghai/Shenzhen taxi sets (no public redistribution link found), NYC/TLC (pickup/dropoff only, no intermediate GPS), OpenTraj (pedestrian/AV-sensor benchmark, not a GPS-fleet source).
  - **Still open:** (a) user action needed to file DiDi GAIA / Grab-Posisi requests — both gated behind an external request form; (b) WorldTrace purity audit, not started. Rome/SF Cabspotting are done (see loader entry below) — no longer open.
  - **Follow-up dataset search (2026-07-26, user flagged Porto's 57.6% sqrt-weighted sampling share as a real generalization risk — none of the other 4 cities are remotely Porto's scale).** Re-searched for a Porto-scale (500k+ traj, single dense city, raw point-level GPS not pickup/dropoff-only) source, direct-verifying every candidate instead of trusting search snippets (same discipline as the original task #19 pass, which downgraded its own top pick on verification). **Result: no new freely-downloadable candidate found — every lead was either the wrong data shape or paywalled, not actually open:**
    - **City-scale Vehicle Trajectory Data from Traffic Camera Videos** (Yu et al., *Scientific Data* 2023; Shenzhen+Jinan, ~5M trajectories, CC-BY-4.0, genuinely free on Figshare, no application) — verified open, but **wrong modality**: points are `NodeID-Time` camera-detection waypoints at road intersections, not continuous noisy GPS. This project's whole pipeline (candidate-radius retrieval, `d_perp` soft targets, off-road noise) assumes raw GPS; intersection-snapped waypoints have already solved the matching problem, they'd need a different ingestion path entirely, not a `parsers.py` entry. Parked, not pursued.
    - **Beijing one-month dataset** (129M points, May 2009, taxi ID + vehicle status) and **Chengdu taxi data** (IEEE DataPort) — both **subscription/institutional-gated on IEEE DataPort**, not freely downloadable despite looking like a strong scale match on paper. Same "looks great, isn't actually open" pattern as the Thessaloniki feed downgraded in the original pass.
    - **Shenzhen ~510k-taxi raw-GPS set** (Zhang et al., cited widely) — real download link untraceable (dead/unlisted citation refs only); the one live GitHub mirror found (`lehaifeng/T-GCN`, "SZ-taxi") is a **different product** — pre-aggregated 15-min road-speed data for 156 roads, not point-level GPS. Confirms task #19's original "no public redistribution link found" verdict, not a new option.
    - **DiDi GAIA** (Chengdu/Xi'an, 2.1M+ trajectories some years) reconfirmed **still application-gated** (`gaia.didichuxing.com`, academic registration + review) — no lighter-weight access path found. Remains, along with Grab-Posisi, the only real lever for a second Porto-scale city, and both still require the user's own account/application action (per the account-creation rule) — not something fetchable directly.
    - **Verdict: the 5-city mix's Porto-heavy weighting (57.6%) is not fixable by finding more free data right now** — it reflects a genuine scarcity of open, raw-GPS, single-city fleets at that scale, not a research gap. Two paths forward if this is worth pursuing further: (1) user files the DiDi GAIA and/or Grab-Posisi request (real payoff, real turnaround-time cost); (2) accept the current mix and lean on `--city-weights` / a lower Porto cap plus more small-to-mid cities (T-Drive/Cabspotting/Rome are already 18.3%/16.6%/5.6% combined ~40%) rather than chasing a single Porto-sized replacement.
- **Multi-city training loader, designed + implemented (2026-07-26, `research2/training/stage2r.py`).** Two architectures considered: (A) keep every city's `build_loader`/`road_z`/`SuccessorTable` exactly as today, one DataLoader per city, and round-robin/weighted-swap which city's context feeds a given step; vs (B) offset every city's `segment_id` into one shared global ID space, merge all graphs+data into one combined dataset so a single batch can mix cities. **Chose (A)**, user-confirmed: the model itself needs zero changes either way (`road_z` is always dim-256 regardless of city — fixed `RoadGAT(out_dim=256)` + one shared `highway_vocab.json` — and `wm_losses`/`extract_features_r`/`RSSM2`/`HeadsLight` already just take `road_z`/`succ` as external arguments), so (A) gets the same shared-model training with far less new code and zero risk to the existing single-city path, at the cost of no within-batch city mixing (each step's batch is single-city, just the step-to-step city varies).
  - **Implementation:** new `train_multi(city_sources, city_weights=None, ...)` + `--multi-city city:source [city:source ...]` CLI flag (`stage2r.py` `main()`); `train()`/single-city path is completely untouched, so every existing script/notebook keeps working unmodified. Per `(city, source)` pair, `build_loader`/`_road_embeddings`/`_successor_table` are called once at startup exactly as the single-city path already does — `ctx[(city,source)] = {road_z, succ, base_ds, it}`. Per step: weighted-random city draw (default **sqrt(dataset-size)-scaled**, so Porto's ~1.6M trajectory-segments don't drown out Rome's 15,854 or `geolife_car`'s 1,816 — overridable via `--city-weights`), pull next batch, rebuild the iterator on `StopIteration` (fresh shuffle) — same recycling logic `train()` already used, just keyed per city. Curriculum length (`curriculum_step`/`curriculum_len`) stays tied to the global step counter and rebuilds every city's loader at the boundary. Logging now shows a per-city rolling-mean loss (last 50 steps) alongside the pooled line, so a broken/degenerate single city is visible immediately rather than averaged away. Checkpoint `meta` records the city/source list + normalized weights actually used (`stage2r_multi_<sources>.pt` naming), mirroring the existing `stoch_groups`/`stoch_classes` provenance convention.
  - **Out of scope, explicit:** `--unfreeze-encoder` (was a one-off closed L3 diagnostic, not worth per-city road_enc complexity) and the TPU/XLA port (`stage2r_tpu.py`) — per-step city switching would force an XLA graph recompile every step since each city's `road_z` has a different shape; a TPU version would need long sticky per-city step-blocks instead, not built.
  - **Verified:** existing `--smoke` regression-checked (exact match to prior numbers, single-city path untouched). New multi-city path smoke-tested end-to-end on CPU with 2 real (small) cities (`romataxi:romataxi` + `tdrive:geolife_car`, confirming `geolife_car` correctly resolves to the reused `tdrive` OSM graph) — both cities loaded, weighted draws alternated correctly, curriculum boundary fired at the right step, checkpoint saved with correct `meta` (`cities`, `city_weights`).
  - **Session 1 result + eval (2026-07-26/27, checkpoint at step 13,217/41,500, `--max-hours 8.5` cap reached cleanly, `eval_multicity_step13217.py`).** After v1-v5 iterated through the cache-path bug (see below) and a subprocess-stdout double-print bug (Kaggle mirrors inherited-fd output — fixed by piping through one `print()` in the notebook's `run_streamed` helper), v6 ran clean: all 5 city caches hit (`cache X:Y: OK`, zero on-the-fly precompute), single-print logs every 500 steps, loss 28.35→21.7 over the session, `kl_act` 0.03-0.04 (healthy, matches production 32×32's 0.010-0.036 range). Resume ckpt uploaded back into `alphaevolve-ckpt` (new file, all 5 pre-existing ckpts preserved). **Porto in-domain match@1 0.8220** (vs production P2 32×32 ceiling 0.8371 @58% train) — encouraging given only 32% trained with capacity split 5 ways, not yet conclusive. **T-Drive match@1 0.7384, up from the Porto-only zero-shot OOD number of 0.6567 — but this is NOT a valid OOD result and must not be reported as "OOD gap closed":** T-Drive is one of the 5 training cities in this mix (18.3% weight), so `eval_ood_tdrive.py`'s "city the model never saw" premise no longer holds for this checkpoint. The honest read is "T-Drive in-domain performance, underweighted, 32% trained" — unsurprising that direct training beats zero-shot transfer. **True cross-city generalization is now untested**: all 5 prepped clean cities are in the training mix, so there is no held-out city left for a genuine OOD check (would need excluding one of the 5 from a future run, or a 6th city). Session 2 (`--resume`) NOT launched this pass — user asked to eval only.
  - **Hannover added as the genuinely held-out cross-city eval target (2026-07-27, user-supplied dataset: Uni Hannover data repository, CC BY-NC 3.0, single driver/one car, Dec 2017-Mar 2019, 617,086 GPS pts / 1,176 raw trips, `gid,trip_id,unixtime,north_utm,east_utm` UTM zone 32N).** Directly answers the "no held-out city left" gap flagged above: NOT added to any training mix, exists purely so `eval_cross_city.py` has a real unseen city to test against. Added `iter_hannover` (`preprocessing/parsers.py`, UTM32N->WGS84 via pyproj, trips already pre-segmented by `trip_id`, no mode-filtering needed) + `hannover` BBOX entries (`preprocessing/schema.py`, `roadgraph/config.py`). Preprocessed clean (1,295 segments, 616,896 pts, near-zero drops — very clean single-source data). **Manifest.json clobbered again by the same known single-source-run gotcha (line 86) — reconstructed the other 5 sources' `segments_out`/`points_out` post-hoc from their parquet files (exact) since the original run's `dropped_*`/`traj_in` breakdown isn't recoverable, only final counts; disclosed in the manifest itself, not silently patched over.** OSM graph built (107,925 segments, 44,280 nodes, 155s). Alignment check: 20 random sampled points, nearest candidate 0.6-10.7m — same quality range as cabspotting/romataxi (0.3-25m).
    - **New tool: `eval_cross_city.py`** — generalizes `eval_ood_tdrive.py` to any city AND reads the checkpoint's own `meta["cities"]` to auto-detect training membership, refusing to label a result "OOD" if the target city was actually trained on (built specifically because the T-Drive result above got misread that way once already). Verified against two known answers before trusting it: multi-city ckpt vs tdrive correctly flags NOT-VALID (weight=0.183, match@1≈0.736, consistent with the direct eval above); single-city P2 ckpt vs tdrive correctly flags genuine OOD (match@1≈0.666, matches the recorded 0.6567 zero-shot number within run-to-run variance).
    - **First genuine cross-city generalization result (multi-city ckpt, step 13,217, on Hannover): match@1 0.7515, OOD gap only -7.05pp vs this ckpt's own Porto in-domain (0.8220)** — much smaller than the Porto-only model's real T-Drive OOD gap (-17.0pp). hit@1/5/15 (0.771/0.711/0.660) and cl_gps (0.217) both come in *better* than Porto in-domain, not just "less bad." **Not a clean controlled comparison, real confounds flagged rather than glossed over:** (a) different target city entirely (Hannover is European/Porto-like; Beijing/T-Drive isn't — the smaller gap may reflect city similarity, not a multi-city-training effect); (b) different checkpoints compared (32%-trained 5-city vs fully-trained single-city, not matched compute); (c) Hannover is one driver's habitual commute routes at clean 1Hz sampling — more repetitive/predictable and less noisy than a diverse taxi fleet, which plausibly explains the better-than-in-domain hit@N/cl_gps on its own, independent of any generalization story. **Directionally promising, not yet a validated claim** — a matched-compute rerun (finish the multi-city ladder to a comparable step/train% as the P2 32×32 ceiling, then re-eval) is the next real test before this becomes a claim rather than a signal.
  - **Session 2 result + eval (2026-08-02, checkpoint at step 24,828/41,500 — 59.8% trained — `--max-hours 8.5` cap reached cleanly, resumed via kernel v7 from the session-1 ckpt already staged in `alphaevolve-ckpt`, no re-upload needed).** **Porto in-domain match@1 0.8386** — now *above* the production Porto-only P2 32×32 ceiling (0.8371 @58% train, plateau band 0.8297-0.8433), at comparable train% (59.8% vs 58%). First multi-city checkpoint to clear the written bar's first condition outright, not just "encouraging." T-Drive match@1 0.7438 (up from 0.7384 at step 13,217) — still **not a valid OOD read** (T-Drive remains a training city, 18.3% weight; same caveat as session 1, not re-litigated as a new finding). **Hannover (genuine held-out city, `eval_cross_city.py`, guard-confirmed `OOD-VALID: True`): match@1 0.7592 (up from 0.7515 at 32% trained), OOD gap -7.94pp vs this ckpt's own Porto in-domain (0.8386)** — both sides of the gap improved with more training (Porto +1.66pp, Hannover +0.77pp) and the gap itself stayed in the same ~7-8pp band rather than widening, i.e. the multi-city generalization signal is holding up under more compute, not just a small-checkpoint fluke. Resume ckpt (final, step 24,828) pulled from the completed kernel and saved to the local model store (`.worktrees/kaggle/ckpt/`). **User called a stop here — no session 3 for now**, so the ladder is paused at 59.8%/41,500 steps, not run to completion. IL-probe (bounded scheduled-sampling/sigma probe, was queued as this session's concurrent job) also stopped by the user mid-run and put on hold, gated on this multi-city run failing or missing its bar — it did neither, so it stays parked.
  - **Loss-curve check (2026-08-02, local, parsed straight from the session-2 kernel log)** before deciding on session 3: per-city rolling-mean training loss moved <0.07 over steps 14,000-24,500 (10,500 steps, ~40% of the session) — already flat, `kl_act` steady 0.03-0.05 (healthy, no collapse). But held-out match@1 still climbed +1.66pp session-to-session over the same window (0.8220→0.8386) — loss-flat does not mean accuracy-flat here, matching this project's own precedent (P1-cont's/P2's flat-loss-but-still-moving-ladder pattern). Gate lifted on IL-probe (see above) partly on `openevolve-researcher`'s recommendation to treat it as independent of the multi-city outcome; once it landed as a decisive win (+1.33pp, not just no-regression), the natural next move was to fold it into the next multi-city session rather than spend a session on the now-stale default recipe.
  - **Session 3 LAUNCHED (2026-08-02, kernel v8, `--max-hours 8.5`), IL-probe recipe folded in: `--ss-max-prob 0.25 --ss-ramp-steps 500 --road-target-sigma 7.0`**, otherwise identical recipe, resuming from step 24,828. **Real bug caught before launch:** the session-2 final ckpt had never been re-uploaded to the `alphaevolve-ckpt` Kaggle dataset (still held the stale session-1/step-13,217 file) — launching as-is would have silently resumed from session 1, discarding all of session 2's progress with no error, the exact failure class already seen once before in this project (line 112, `RESUME_SRC` pointing nowhere). Fixed: downloaded the live 6-file dataset, swapped in the verified step-24,828 ckpt (torch-loaded and step-checked before upload, not just file-copied), re-versioned the dataset, confirmed `ready`, then pushed the kernel. **Bar:** Porto in-domain match@1 and Hannover OOD match@1 should each hold or improve vs their session-2 values (0.8386 / 0.7592) with the OOD gap not widening past its current ~7-8pp band — the IL-probe recipe was only validated on single-city Porto, so this session doubles as its first test under the multi-city data mix.
  - **Session 3 result + eval (2026-08-03, checkpoint at step 36,309/41,500 — 87.5% trained — `--max-hours 8.5` cap reached cleanly, kernel v8 COMPLETE, log clean throughout: `kl_act` steady 0.033-0.051, no collapse, lr correctly deep in cosine decay by the end).** **Porto in-domain match@1 = 0.8540** (session 2: 0.8386, **+1.54pp**) — now +1.69pp above the Porto-only P2 32×32 ceiling (0.8371), the largest margin yet. T-Drive 0.7696 (up from 0.7438) — still **not a valid OOD read**, T-Drive remains a training city (18.3% weight), not re-litigated. **Hannover (genuine held-out city, `eval_cross_city.py`, guard-confirmed `OOD-VALID: True`): match@1 = 0.7678** (session 2: 0.7592, **+0.86pp**). **Bar read, honest: first condition (both metrics hold/improve) clearly PASSES — both improved, Porto decisively so. Second condition is a soft miss: OOD gap = 8.62pp (was 7.94pp), +0.68pp wider than session 2, just outside the ~7-8pp target band** (Porto grew faster than Hannover this session — 1.54pp vs 0.86pp — so the gap crept up even though both sides moved in the right direction; not a red flag at this magnitude, but flagged rather than rounded away per this project's own discipline). No verdict change to the multi-city direction — it's still the best-performing checkpoint on both individual metrics recorded so far. Resume ckpt (final, step 36,309) pulled from the completed kernel, saved to `.worktrees/kaggle/ckpt/`, and re-uploaded to `alphaevolve-ckpt` (torch-verified step 36,309 before upload, dataset status `ready`). **Tooling note:** `kaggle datasets version` deterministically failed (`FileNotFoundError` on its local resumable-upload tracking JSON) when invoked from the Bash tool's Git-Bash environment with forward-slash Windows paths, regardless of staging directory — root cause not fully isolated, but the workaround is confirmed: run the same command from PowerShell with a native backslash path, which uploaded all 6 files and created the version cleanly on the first try. **No session 4 launched — awaiting explicit go-ahead**, per the standing stop-and-check convention on multi-hour Kaggle runs.
  - **Session 4 LAUNCHED (2026-08-03, kernel v9), closing out the schedule: 5,191 steps left (36,309→41,500), same recipe/config unchanged, `--max-hours 8.5` cap (safety buffer only — should finish naturally in ~3.85h at session-3's observed 2.67s/step).** Resumes from the mounted `alphaevolve-ckpt` dataset, already re-versioned with the torch-verified step-36,309 ckpt. Goal: final ladder verdict for this run (multi-city + IL-probe recipe combined) at 100% of the 41,500-step schedule, rather than stopping the read at 87.5%.
  - **Session 4 result + eval (2026-08-03, checkpoint at step 41,499/41,500 — schedule COMPLETE, kernel v9 COMPLETE, log clean, `kl_act` steady 0.032-0.053 no collapse, lr correctly decayed to ~0 by the end).** **Porto in-domain match@1 = 0.8527** (session 3: 0.8540, **-0.13pp**, flat within noise). T-Drive 0.7645 (still not valid OOD). **Hannover (genuine held-out, guard-confirmed): match@1 = 0.7660** (session 3: 0.7678, **-0.18pp**, flat within noise). **OOD gap = 8.67pp (session 3: 8.62pp, +0.05pp — stabilized, NOT continuing to widen).** **Verdict: the final 12.5% of the schedule (36,309→41,500) plateaued — both metrics moved by less than 0.2pp, consistent with this project's established pattern of late-cosine-decay flattening (P1-cont sessions 4-5, P2's own 58%-100% flat band). This is the final ladder verdict for the multi-city + IL-probe run, not an interim read: full ladder = session1(32%) Porto 0.8220/Hannover 0.7515/gap 7.05pp → session2(60%) 0.8386/0.7592/7.94pp → session3(87.5%) 0.8540/0.7678/8.62pp → session4(100%) 0.8527/0.7660/8.67pp.** Best Porto/Hannover numbers are session 3 and 4, tied within noise; both clear session 2's numbers decisively (+1.4pp Porto, +0.7-0.9pp Hannover) and both sit comfortably above the Porto-only P2 32×32 ceiling (0.8371). Ckpt (final, step 41,499) pulled, saved to `.worktrees/kaggle/ckpt/`, re-uploaded to `alphaevolve-ckpt` (torch-verified step 41,499, dataset `ready`). **Schedule for this run is DONE — no session 5 without a new recipe change to justify it** (task #26 complete).
  - **Kubicka GT re-check (2026-08-03, local CPU/GPU, zero Kaggle quota, `archive/eval_gt_kubicka_wm.py`, same 8 clean tracks as the 2026-07-11 check) — confound found and isolated, not a clean "did the model improve" read.** Session-3 ckpt (step 36,309) against the 8 tracks: nk 0.8986/0.9001 (mean/pooled), hybrid 0.8908/0.8923, online 0.9005/0.8991 — all ~7pp below the 2026-07-11 numbers (nk 0.9703, hybrid 0.9698, online 0.9759). **`nk` is pure classical geometry (NK-HMM), independent of any WM checkpoint** — it dropping by the same magnitude as hybrid/online is a red flag that the baseline itself moved, not the model. **Control run: old zero-shot Porto-only ckpt (`stage2r_porto_run2xl_16x16_final.pt`, the exact ckpt the 2026-07-11 numbers were measured on) through TODAY's codebase, same 8 tracks — nk 0.8986/0.9001 (identical to the new ckpt), hybrid 0.8875/0.8857, online 0.9005/0.8991 (both effectively identical to the new ckpt's numbers).** **Confirmed: this is a codebase/graph-drift confound, not a WM regression** — something upstream of the checkpoint (most likely the `kub<tid>` OSM subset graphs being rebuilt/touched by this session's other city-graph work, or a matcher/candidate-index change) moved the baseline from ~0.97 to ~0.90 for BOTH old and new checkpoints identically. **The multi-city vs Porto-only comparison is still valid within this run** (same drifted baseline for both): new ckpt's hybrid-vs-nk gap is −0.78pp (0.8923 vs 0.9001) vs the old ckpt's −1.44pp (0.8857 vs 0.9001) — a small, noisy-at-n=8-tracks signal that the multi-city ckpt's learned emission is closer to (not further from) geometry on this GT set, consistent with the hybrid-vs-nk OOD-narrowing pattern already seen elsewhere (T-Drive: −0.8pp at 16×16 → −0.47pp at 32×32). **Not chasing the drift's root cause right now** (low priority — the historical 0.97 number and the 2026-08-03 ~0.90 number are simply not comparable to each other; future GT checks should compare against this session's ~0.90 baseline, not the 2026-07-11 one, until/unless the drift is diagnosed).
  - **Real run LAUNCHED (2026-07-26, Kaggle T4, `alphaevolve-multi-city-t4`, kernel v1).** Two new Kaggle datasets created to get the 4 non-Porto cities onto Kaggle without touching the existing Porto-only datasets (avoids re-uploading Porto's 1.8GB unnecessarily): `alphaevolve-osm-hybrid-cities` (tdrive/cabspotting/romataxi OSM graphs + shared `highway_vocab.json`, 87MB) and `alphaevolve-gps-hybrid-cities` (tdrive/geolife_car/cabspotting/romataxi processed GPS, ~940MB). Notebook (`.worktrees/research2/notebooks_multicity/`) merges these with the existing porto-only `alphaevolve-osm-graphs`/`alphaevolve-gps-processed` datasets into one local `OSM_ROOT`/`PROC_ROOT` via symlinks (no copying) before calling `--multi-city porto:porto tdrive:tdrive tdrive:geolife_car cabspotting:cabspotting romataxi:romataxi`. Recipe is single-variable vs the production P2 32×32 baseline: same 32×32 latent, same `--steps 41500`/batch 256/lr 4e-4 schedule, fresh init (no warm-start) — only the training DATA MIX differs. Resume-chained on its own file (`stage2r_multi_porto-tdrive-geolife_car-cabspotting-romataxi.pt`) across sessions exactly like P1-cont/P2. **Bar:** beat the Porto-only P2 32×32 ceiling on Porto's own held-out split, and/or meaningfully close the T-Drive OOD gap (currently −17.0pp zero-shot) since the WM now trains on non-Porto traffic directly — judged via `eval_research2.py` and `eval_ood_tdrive*.py` once a session completes.
  - **User caught a real quota risk and cancelled v1 (2026-07-26): only 9.40h Kaggle GPU quota remained this week, v1 was launched at `--max-hours 10.0` alongside the concurrent IL-probe kernel (§ below) — combined ask exceeded remaining quota. User asked for one review pass before relaunching at `--max-hours 9.3`.** Review (manual, not `/code-review ultra` — that command is user-triggered/billed and cannot be launched by the assistant) found the IL-probe kernel had already failed for a real, related reason: its CONTROL cell used `--out-dir /kaggle/working/ckpt_control` while the pre-seeded Porto candidate cache lived under `/kaggle/working/ckpt/cache/` — a directory mismatch, so `build_loader` found no cache and silently attempted a full from-scratch candidate precompute over Porto's 82M points inside the GPU-metered session, dying `SIGKILL` (OOM) at 202.8s (confirmed from the downloaded kernel log — the exact `project_kaggle_oom_cache_fix.md` failure signature). Checking the multi-city notebook for the same bug class found it present there too, plus two more: (1) **cache-path mismatch** — `train_multi` namespaces caches under `cache/{city}__{source}/` (`stage2r.py` line 449-450) but the notebook pre-seeded the flat `cache/porto_n1658437_r50_k10.npz`, which would have forced the same OOM-risking full Porto precompute inside the 9.3h GPU session instead of reusing the cache; (2) **`RESUME_SRC` pointed at `/kaggle/working`** (always empty on a fresh container) instead of the mounted `alphaevolve-ckpt` input dataset — every future "resume" session would have silently restarted from scratch, discarding all progress with no error; (3) cleanup cell's per-city cache-dir names were wrong (dead code, harmless — the next line's blanket `rmtree` already covered it). All three fixed in the notebook; kernel v2 pushed and RUNNING with `--max-hours 9.3`. IL-probe's cache-path bug logged but not yet fixed/relaunched (not this turn's ask).
- **IL-improvement pass (2026-07-26, user goal: "do all the free/cheap and expensive changes which will help improve IL").** Reframed the road-head/WM-prior training as what it structurally already is — imitation learning (behavior cloning against trace-derived pseudo-labels) — then diagnosed and fixed the two concrete gaps found:
  - **Diagnostic first (local GPU, `diag_jump_vs_priorerror.py`, promoted P2 32×32 ckpt, full 500-traj held-out split, zero training, ~85s):** tested whether the online `hyb+topo` disconnected-jump rate is a training/inference conditioning-mismatch artifact (train always teacher-forces the ground-truth pos_idx pseudo-label as previous-state; `match_online` closes the loop on the model's own previous pick) before spending any implementation effort on a fix. **Result: P(jump | previous step was a miss) = 0.1644 vs P(jump | previous step was a hit) = 0.0366 — 4.49x higher, two-proportion z=28.98 (n=16,096 / 3,498).** Decisively confirms the mechanism: one wrong pick makes the next-step jump ~4.5x likelier. Green-lit scheduled sampling on this evidence (would have been unjustified as a blind bet — this is exactly the kind of check the fork/kl_act diagnostics earlier this session came back negative on, so it was not assumed to work here either).
  - **Scheduled sampling / DAgger-style fix, IMPLEMENTED (`training/stage2r.py`).** During teacher-forced unrolling, with schedule probability `p` (0 at step 0, ramped to `--ss-max-prob` by `--ss-ramp-steps`), the previous-step `road_embed` fed into `rssm.step` is swapped from the ground-truth pos_idx-derived embed to the model's OWN previous posterior road-head argmax pick, per-sample-independent Bernoulli draw. Zero new params (pure data-flow change inside the unroll), so it's resume-compatible with any existing 16×16/32×32 ckpt as a **continued-training** experiment, not a from-scratch one. `--ss-max-prob 0` (default) is exactly the old behavior, byte-identical gradients — verified via `--smoke`.
  - **Known risks logged, not hand-waved (Huszár 2015's scheduled-sampling-is-a-biased-objective critique; pos_idx pseudo-labels are themselves noisy so "ground truth" is approximate; interacts with an already-fragile posterior — kl_act 0.010-0.036 on the 32×32 ckpt).** Written bar before launch: online jump rate must drop **without** a hybrid/road-head match@1 regression >0.5pp vs the current promoted ckpt continued the same number of steps with `--ss-max-prob 0` (matched-compute control, not the original from-scratch numbers — isolates the scheduled-sampling variable from further training alone). Bounded probe (short continued-training run, Kaggle, both `--ss-max-prob 0` control and `0.25` treatment from the same P2 32×32 ckpt) queued next.
  - **ASTRA-style pseudo-label sharpening, IMPLEMENTED (cheapest lever from the 2026-07-26 P2 mid-run literature check, `literature_papers/astra_self_training_weak_supervision.json`).** The road-CE soft target is `softmax(-d_perp²/2σ²)` with a fixed `σ=10m` (`training/stage2.py:_soft_road_ce`) — this is exactly the "noisy weak-supervision soft label" ASTRA's minimal-viable lever targets: lower the target temperature (smaller σ) to sharpen/peaken the pseudo-label distribution, raising its information content with zero new data, params, or architecture change. Added an optional `sigma` param (default 10.0, unchanged) to `_soft_road_ce` and matching param to `stage2r.py:_road_target_entropy` (kept in sync per the existing code comment), threaded as `--road-target-sigma` through `train`/`train_multi`. Default-path byte-identical; `--smoke` re-verified.
  - **Bar for the sigma probe:** a sharper σ (e.g. 6-7m) should lower `tgt_ent` (soft-target entropy, the CE floor) and raise `match@1`/lower `cl_gps` at matched compute vs σ=10 control — same continued-training matched-compute protocol as the scheduled-sampling probe, cheap enough to run as a short bounded continuation rather than a full from-scratch ladder rung.
  - **Not done (explicit, out of scope this pass):** re-tried `topo-hard` (already tested 2026-07-11, rejected — trades −1.5pp match@1 for only −0.6pp fewer jumps, dominated by `hyb+topo`, a known negative, not re-litigated).
  - **Probe LAUNCHED (2026-07-26, Kaggle T4, `alphaevolve-il-probe-t4`, kernel v1 RUNNING).** Two short (`--max-hours 1.5` each) branches from the SAME promoted P2 32×32 base ckpt (`stage2r_porto_p2_32x32.pt`, step 24,231), same `--steps 41500`/batch/lr so the resumed cosine schedule's own saved state (incl. `T_max`) stays consistent: CONTROL (`--ss-max-prob 0 --road-target-sigma 10`, i.e. more of the identical old recipe) vs TREATMENT (`--ss-max-prob 0.25 --ss-ramp-steps 500 --road-target-sigma 7`), separate `--out-dir`s so neither overwrites the other. `alphaevolve-code-research2` re-versioned first with the scheduled-sampling/sigma-sharpening code. Eval happens locally afterward (not in-notebook, matching this project's established train-on-Kaggle/eval-locally split) via `diag_jump_vs_priorerror.py --ckpt <path>` (extended to accept a direct `.pt` path, not just a table key; also now prints the unconditional overall jump% for direct comparison to the historical hyb+topo numbers) against both downloaded checkpoints — compares treatment vs control's jump% and match@1 per the written bar above. Notebook: `.worktrees/research2/notebooks_il_probe/`.
  - **v1 crashed at 202.8s (2026-07-26/27) — same cache-path bug class as the multi-city notebook (line 112), diagnosed then but never actually fixed in this notebook.** CONTROL/TREATMENT each got their own `--out-dir` (`ckpt_control`/`ckpt_treatment`), but `train()`'s single-city cache namespacing is `{out_dir}/cache` (`stage2r.py:282`) while the notebook only ever staged the pre-built cache under the shared `ckpt/cache/` — neither branch's `--out-dir` matched, so `build_loader` silently fell back to a from-scratch precompute over Porto's 82M points inside the metered GPU session and OOM'd. **Fixed (2026-08-02):** added a `stage_cache(out_dir)` helper called once per branch, copying the pre-built cache into that branch's own `cache/` subdir, plus a fail-fast assert if the cache is ever missing (refuse to risk an in-session OOM instead of silently falling back). Kernel v2 relaunched, ran clean past 202.8s — user manually stopped it mid-run to gate further compute spend on other priorities (multi-city session 2 was running concurrently at the time).
  - **Gate lifted, kernel v3 relaunched and RUN TO COMPLETION (2026-08-02)** on the recommendation of an independent `openevolve-researcher` review (asked to challenge the project's next-move options): the IL-probe targets a decisively diagnosed bug (z=28.98) independent of the multi-city data-mix lever, so it didn't need to wait on that run's outcome. CONTROL reached step 26,106, TREATMENT step 26,027 (~matched compute, 1,875-1,796 steps past the shared step-24,231 base). **Written-bar result, `diag_jump_vs_priorerror.py` against both, N=500 held-out trajs:**

    | | jump% (unconditional) | match@1 |
    |---|---|---|
    | CONTROL (`ss=0, σ=10`) | 6.09% | 0.8199 |
    | TREATMENT (`ss=0.25, σ=7`) | **5.72%** | **0.8332** |

    Bar was "jump% drops without match@1 regressing >0.5pp" — treatment cleared both independently (jump% −0.37pp absolute / ~6% relative; match@1 **+1.33pp**, not just "no regression"). Both branches independently re-confirm the underlying diagnosis at this later step too (miss/hit jump ratio 4.35x control / 4.44x treatment, z≈28 both, matching the original 4.49x/z=28.98 finding almost exactly — not a fluke of the original 500-traj sample). Scheduled sampling + sigma-sharpening together are a genuine, decisive win, not a marginal one. Checkpoints saved to `.worktrees/kaggle/ckpt/stage2r_ilprobe_{control,treatment}.pt`. **Next candidate step (not yet decided/launched):** promote the treatment recipe (`--ss-max-prob 0.25 --ss-ramp-steps 500 --road-target-sigma 7`) into the next multi-city training session, since both levers are additive/orthogonal to the data-mix change and nothing here contradicts that.

- **"Push past 0.9" literature challenge (2026-08-02, `openevolve-researcher`, user goal: reconsider architecture/policy/loss, explain why RSSM struggles, evaluate data/capacity scaling further).** Explicit request to challenge, not validate. Findings:
  - **Q1 — is 0.9 on the current proxy metric (tolerant Hit@1, strict held-out split) even a coherent target? Likely not, as stated.** Literature survey (DiffMM arXiv:2601.08482, DeepMM, RNTrajRec ICDE'23, HSTGMatch, transformer-as-MT arXiv:2404.12460) shows reported 90%+ accuracy clusters almost entirely on self-referential GT (label = a denser version of the same signal matched by another algorithm — DiffMM's own Porto/Beijing setup) or undisclosed-methodology real GT (HSTGMatch, 88.6-90.4% F1, GT construction not stated). Independently-audited real GT (this project's own Kubicka zenodo-57731 check) or harsh synthetic noise (DeepMM at 60s/100m: only 66%) both land well below 0.9 — DeepMM's own HMM baseline collapses 83.8%→40.0% as sparsity increases on the SAME Porto data this project trains on. **This project's proxy metric sits at the harsh/independent end of that spectrum; its Kubicka GT re-run (0.970-0.976, 8 clean tracks) already matches or beats the field's cleanest reported numbers.** Verdict: stop chasing 0.9 on the proxy metric as a number in isolation — `road_ex` (distance to the pseudo-label's own entropy floor) and periodic Kubicka GT re-checks on the current best ckpt are the more meaningful signals.
  - **Q2 — pseudo-label noise ceiling: no tighter bound exists than what's already tracked.** `tgt_ent` is exactly the Gibbs'-inequality CE floor for the chosen soft target — mathematically already the right number, Kendall & Gal (NeurIPS'17) aleatoric/epistemic framing confirms the `tgt_ent`/`road_ex` split is the correct one. **New risk surfaced, not previously flagged: the floor is a function of the chosen sigma, not of real-world truth.** Sharpening sigma (the ASTRA lever just proven) is mathematically identical to lowering distillation temperature — trades label ambiguity for label confidence, and confident-wrong labels near intersections/parallel roads is the failure mode, not a benefit, if pushed too far. Only the independent Kubicka GT check (not road_ex) can tell if the pseudo-labeler itself is drifting from truth. Actionable, cheap, not yet done: adapt Confident Learning (Northcutt et al., JAIR'21, arXiv:1911.00068) as a post-hoc CPU-only diagnostic on an existing checkpoint to explicitly split road_ex into "structural label noise" vs "real model gap."
  - **Q3 — ranked unexplored levers**, by literature grounding in this exact task family (GPS→road-segment classification): (1) **contrastive auxiliary loss** on road/trajectory latents — directly precedented (Mao et al. CIKM'22 arXiv:2209.06389; RLOMM arXiv:2502.06825 folds this in explicitly) — cheap, no new params beyond a projection head, reuses already-computed latents; (2) **successor-conditioned topology auxiliary loss** (predict topologically-reachable candidate set at t+1, not just the correct segment) — grounded in why GraphMM/HSTGMatch beat sequence-only baselines; (3) **small-window beam/lookahead decode for the ONLINE path** (closes the same offline/online gap CRF/Viterbi already closes offline at 0.8679 vs online 0.8287) — not a new idea, extends the existing hybrid-Viterbi mechanism to a bounded lookahead instead of full-trajectory; (4) Confident-Learning diagnostic (Q2, free); (5) uncertainty-aware ensemble decoding — ranked lowest, ~2x inference cost, no map-matching-specific precedent found. **Widened MLP heads (already queued) has NO literature precedent found as a decisive lever for this task** — downgrade from "proven-analogous" to "cheap, low-confidence experiment," matched-head-width-to-GRU is a generic Transformer heuristic, not evidenced here.
  - **Q4 — is the DreamerV3-lineage RSSM still justified with P2/RL closed? Split verdict, stated plainly.** Every SOTA discriminative matcher found (GraphMM, RNTrajRec, HSTGMatch, DiffMM, DeepMM, RLOMM) uses a deterministic architecture — none uses a Dreamer-style stochastic categorical world model — real signal the field doesn't believe a stochastic latent WM is required for matching accuracy alone. Lambert et al. ("Objective Mismatch in MBRL," L4DC'20) is the exact citation for what this project already found empirically: a world model's one-step predictive objective is frequently uncorrelated with downstream task performance — the RSSM's original alignment target (RL policy quality) no longer exists now that P2/RL is closed, so its remaining justification must be re-earned on its own terms, not inherited from the original P2 framing. **Counter-case, and the reason NOT to rewrite:** "How Should World Models Be Evaluated for Embodied Decision-Making?" (arXiv:2606.15032) argues predictive/generative latents earn their keep specifically for tasks needing recursive rollout beyond the observed trajectory — exactly what path PREDICTION is (and none of the cited discriminative SOTA matchers attempt forecasting, only recovery/matching of already-traversed points). This project's own recorded result (WM prior rollouts beat HMM β=20 at every horizon) is the RSSM concretely paying rent under this exact criterion. **Verdict: don't rewrite (no new evidence reopens that door — every SOTA method above is a supervised-GT-label architecture, not a drop-in for label-free training, the project's actual differentiator). Do run one cheap, single-variable, lineage-consistent ablation: read the matching head off the GRU's deterministic recurrent state directly (bypass the categorical sample for matching only, keep the full stochastic RSSM driving prediction/rollout as today).** If deterministic-state-only matching accuracy matches or beats the current road-head, that's direct evidence the stochastic latent is being carried for prediction's benefit only, not matching's — clarifies which component earns its compute budget for which product metric, per this project's own §7 logging convention. No paper found that runs this exact ablation — it is a project-native experiment, not an answered question.
  - **Not yet decided/launched:** any of Q3's levers, the Q4 deterministic-state ablation, or a fresh Kubicka GT re-check on the current best (multi-city session 2/3) ckpt. Full agent report (42 tool calls, 20 sources) available via SendMessage to agent `a0ae6ce844d1ef3e4` if deeper follow-up is needed.
- **"Loss/policy/data/capacity" literature challenge, round 2 (2026-08-03, `openevolve-researcher`, post-session-4-plateau, user goal: loss changes? bootstrapped/Monte-Carlo synthetic GPS data? more capacity? challenge every idea).** Builds on the round-1 findings above, doesn't re-derive them. Verdicts:
  - **Loss:** label smoothing and the existing road-target-sigma sharpening pull in opposite directions mechanistically (smoothing flattens confidence, sigma sharpens it) — don't stack them, one cheap swap-test to confirm. Focal loss has weak/inconsistent precedent on non-heavily-imbalanced data and specifically risks amplifying the sharpened pseudo-labels' own errors (known focal-loss-plus-noisy-label failure mode) — low-prior, cheap to rule out. Strongest new finding: DreamerV3 itself states its performance "predominantly rests on" the reconstruction loss this project already had to drop for posterior collapse — i.e. the decoder-light recipe may be missing DreamerV3's dominant training signal with nothing substituting for it. Two concrete non-reconstruction substitutes with real precedent: **R2-Dreamer**'s redundancy-reduction (Barlow-Twins/VICReg) self-supervised loss (decoder-free and negative-sampling-free, low VRAM risk), and **masked segment/trajectory prediction** (Toast/CTLE/MRRT/RED precedent, RED explicitly ablates masking ratio's effect on downstream accuracy). Contrastive road-vs-trajectory-latent loss (Q3's lever #1) re-confirmed via RLOMM, which uses it as a separate weighted term alongside its RL loss in a live SOTA system — but RLOMM's own ablation isolating the contrastive term's contribution wasn't extractable from available text (flagged as a real gap, not assumed).
  - **Policy/RL — dead end for a standalone actor, confirmed again; one narrow residual-RL trial is the only thing left worth running before closing the question for good.** RLOMM/MIDIRL do NOT selectively apply RL to hard candidates only (both apply it across the whole decision sequence) — no real precedent found for that specific framing. What does have precedent: **Residual Policy Learning** (Silver et al. 2018) and **Jump-Start RL** (Uchendu et al. 2023) — RL learns only a small correction on top of the frozen road-head base policy, with the structural property that it can't regress below the base policy's performance. Worth exactly one bounded, capped trial (reuses existing RL actor infra, gated to low-confidence road-head outputs, zero-initialized) — if even this favorable, can't-lose-worse setup shows no gain, that's a more final verdict than the current standalone 0.593 result.
  - **Bootstrapped/synthetic GPS data — do not prioritize.** DeepMM's synthetic GraphHopper trajectories were a label-scarcity fix for a *supervised* method; this project is already self-supervised on 5 real cities, so that motivation doesn't transfer. DiffTraj/DP-TrajGAN-family generators are built for *privacy-preserving publishing*, not downstream-accuracy gains, and DP variants explicitly trade utility for privacy. Mechanistically, any generator trained on the same 5 cities can only interpolate their existing distribution — combined with this project's own on-record OOD-transfer failure (T-Drive semantic context, −21.9pp) and current 8.67pp Hannover gap, synthetic in-distribution data is likelier to deepen in-domain overfitting than close the OOD gap. **The more defensible "Monte Carlo" lever: MC-dropout uncertainty-gated pseudo-labeling** (UPET precedent) — gate scheduled-sampling/sharpened targets by model uncertainty instead of accepting every sharpened pseudo-label uniformly, directly targeting the round-1 Q2 risk (sigma-sharpening compounding overconfident wrong labels). Noisy Student (Xie et al. 2020) reinforces that self-training gains come from real unlabeled data, not synthetic data.
  - **Capacity — not supported by current evidence; the plateau's own shape says schedule artifact first, not ceiling.** Model was still climbing solidly through 87.5% of the schedule (+1.54pp session 3) and only flattened in the final 12.5% — matches this project's own prior late-cosine-decay pattern (P1-cont, P2), not a capacity signature. Correct diagnostic before touching architecture: train-vs-val gap at the plateau (small+flat → schedule/data-limited; growing → overfitting, where widening would make it worse). Scaling-law literature adds a specific caution: model-size-vs-loss is U-shaped at a FIXED data budget — this project's 5-city dataset is fixed, so widening now risks hitting the overfitting inflection sooner, not extending real headroom. DreamerV3's own 12M→400M monotonic-scaling result doesn't transfer cleanly — it was paired with more data/interaction, not a fixed schedule.
  - **Ranked next experiments (cheapest/highest-signal first, full table + sources in agent report):** (1) train-vs-val plateau diagnostic, zero compute, offline log analysis; (2) re-run final leg with slower cosine decay/extra low-LR steps, resuming existing ckpt — directly tests schedule-artifact hypothesis; (3) deterministic-state-only matching ablation (Q4 lever, now cheaper-framed — likely cheaper than current path since it skips stochastic sampling); (4) contrastive road/trajectory-latent auxiliary loss (prefer VICReg/Barlow-Twins variant over contrastive if VRAM-constrained); (5) MC-dropout uncertainty-gated pseudo-labeling on existing real data; (6) bounded residual-RL correction, zero-initialized, gated to low-confidence outputs; (7) focal loss swap-test (low prior); (8) small controlled synthetic-trajectory pilot, explicitly checking Hannover OOD gap for widening, not just Porto in-domain (low prior, only if 1-7 don't move the needle). Widening capacity (9) explicitly last, gated behind (1) and (2) both failing to explain the plateau. Full report incl. all citations via SendMessage to agent `acb1765eab0dafaf7`.
- **Imitation-learning next-steps challenge, round 3 (2026-08-03, `openevolve-researcher`, user goal: research IL-lens next steps for matching + prediction, don't stop until valuable insights).** Five hypotheses attacked, not validated. Verdicts:
  - **H1 (PURSUE, modified) — DAgger-proper via Viterbi relabeling.** Scheduled sampling is only DAgger's "visit own states" half and is a provably inconsistent objective (Huszár arXiv:1511.05101) with no error bound; true DAgger's no-regret reduction gives O(Tε) vs BC's O(T²ε) (Ross & Bagnell AISTATS 2011). This project HAS a queryable expert: offline Viterbi. **Circularity risk is real** — hybrid expert's emission uses the student's own road head — so run **pure NK-HMM Viterbi (0.8447, fully student-independent) as the first relabeling arm**, hybrid (0.8655) only as a second arm gated on the first showing lift. No map-matching paper trains on Viterbi-decoded path pseudo-labels (searched — genuinely unclaimed), but the pattern = Sequence-Level KD (Kim & Rush EMNLP 2016, beam-teacher output as target) and is the next rung of the ASTRA ladder already climbed once (σ-sharpening). **Convergent evidence from ASR: offline non-causal teacher → streaming causal student distillation is a mature sub-field** (arXiv:2308.16415, arXiv:2103.00422, Google unsupervised-data distillation; ~16% relative WER gains) — structurally identical to distilling hybrid Viterbi into `matcher.py`'s online head. Honest limit: DAgger fixes only the distribution-shift share of the 5.3pp online/offline gap, not the information share (offline sees the whole trajectory) — expect shrink, not close.
  - **H2 (KILL for now) — MaxEnt IRL for prediction.** Ziebart 2008 is literally this task's ancestor, and Google's RHIP (ICLR 2024, arXiv:2305.11290) scaled IRL to Maps — but its 16-24% gain is vs Google's own prior baseline with NO reported strong-neural-baseline comparison anywhere accessible, and plain soft value iteration needed a new algorithm to scale even for Google. WM prior already beats HMM at all horizons; replacing a working cheap component to chase an unproven edge fails cost/benefit. Bounded rerank-probe kept as item 4 only if Viterbi-relabeling plateaus prediction.
  - **H3 (KILL outright) — professor forcing.** No precedent outside text/handwriting/video-gen; 2025 successors (Self/Rolling/Next Forcing, arXiv:2506.08009/2509.25161/2606.11187) explicitly discard the adversarial hidden-state discriminator in favor of direct self-conditioning — which IS the mechanism this project already ships. GAN loss on a kl_act 0.03-0.05 posterior after this project's collapse history = bad risk/reward, no trial.
  - **H4 (KILL, subsume) — self-imitation (SIL, Oh 2018).** No scalar return to filter on here; "rollout matched realized future" is DAgger-relabeling reframed. If used at all: one filtering rule inside H1 (keep self-rollouts the offline Viterbi retrospectively agrees with), not a track.
  - **H5 — IQ-Learn/SEARN/LOLS: theoretically relevant (LOLS can beat the reference policy), not worth a session before H1; ASR distillation was the actionable hit (folded into H1 above).**
  - **Ranked IL experiments:** (1) NK-only-Viterbi relabel arm — CPU relabel <1h local + 1 Kaggle session; bar: beat 0.8332 match@1 by >0.5pp or jump 5.72% by >0.3pp, else H1 dead entirely; (2) hybrid-Viterbi arm only if (1) lifts; must beat (1) by >0.3-0.5pp to justify circularity, tie/loss → NK-only permanent; (3) repurpose round-2's MC-dropout gating as **Viterbi-decode-margin gating** (trust relabels only where path score margin high; offline CPU analysis first, no retrain); (4) bounded IRL-lite rerank probe, only on prediction plateau, reusing the closed RL track's r_dist/r_topo/r_head as reward features. **Merge notes:** H1 sequences after (not parallel to) the σ-ladder; MC-dropout gating repurposed not superseded; orthogonal to round-2's schedule/capacity axes (can share Kaggle weeks); residual-RL further weakened by H3's finding. No 3+ pillar competitor surfaced (Google RHIP = P2/P3-adjacent at most, background reference). Full report via SendMessage to agent `a7b3f08f24f16715f`.
- **NK-Viterbi relabeling IMPLEMENTED (2026-08-03, IL round-3 experiment #1, task #28) — code + self-checks done, NOT yet run.** Supplies the relabeling half of DAgger that scheduled sampling alone does not: the road-CE target stops being purely the topology-blind per-point projection and blends in the offline expert's whole-trajectory decode.
  - **`training/relabel_viterbi.py` (new).** Reuses the canonical NK Viterbi/transition/emission from `hmm_baseline/baseline_hmm.py` rather than duplicating it; emits one int64 `.npy` per city:source holding the expert-chosen SEGMENT ID per fix (-1 = expert abstained). Segment id, not candidate slot, deliberately — dataset Change 7A re-permutes the K slots on every `__getitem__`, so a stored slot index would be stale on read. Reads `ds._cand` directly (unpermuted, df-row-aligned) instead of going through `__getitem__`. **Expert is pure NK (0.8447), not the stronger hybrid (0.8655), on purpose** — hybrid's emission is the student's own road head, which would make relabeling a self-confirmation loop and break the exogenous-expert assumption behind DAgger's O(T·ε) bound; hybrid is arm 2, gated on arm 1 showing lift.
  - **`stage2.road_soft_target` (new, single definition).** `tgt = (1-a)·softmax(-d²/2σ²) + a·onehot(viterbi seg)`. Both `_soft_road_ce` (the loss) and `_road_target_entropy` (its floor, feeding `road_ex`) now call it, so the metric can no longer describe a different target than the one being trained on — the old pair was kept in sync only by a code comment. Fixes fall back to pure geometry where the expert abstained or its segment isn't among that fix's K candidates. **`--viterbi-alpha 0` is bit-identical to the old target (asserted in `--smoke`), so the control arm is genuinely the current recipe.**
  - **Threading:** `viterbi_path` through `TrajectoryGraphDataset`/`build_loader`, `viterbi_seg` through `collate_fn` (padded **-1**, not 0 — 0 is a real segment id), `extract_features_r`, `wm_losses`; `--viterbi-dir`/`--viterbi-alpha` on both `train` and `train_multi`, recorded in ckpt `extra_meta`. `_viterbi_path` **raises** if `--viterbi-alpha > 0` but the label file is missing, rather than silently running the control recipe under a treatment-arm name — same silent-no-op class as the cache-path and `RESUME_SRC` bugs already hit twice here.
  - **Verified (all local, seconds, zero GPU):** `stage2r --smoke` passes in both `.worktrees/research2` and `src/` with identical numbers (α=0 target unchanged; α=0.5 moves exactly 0.500 mass onto the expert slot; abstain rows untouched; α=0.5 path trains without NaNs). `relabel_viterbi --self-check` builds a 3-fix case where a parallel decoy road is geometrically nearest at the middle fix and asserts topology overrides it (`nearest=[0,9,2] → viterbi=[0,1,2]`). `dataset.trajectories` self-check extended to assert label rows stay aligned with df rows through the slot permutation and collate padding, and that no `viterbi_path` leaves the key absent. Also caught/fixed a read-only-memmap-view hazard in the label slice.
  - **Kaggle plumbing:** `notebooks_relabel/` (CPU kernel, `enable_gpu: false` — no checkpoint is loaded, so this costs zero GPU quota, same as the candidate-cache precompute); `upload_datasets.sh` now stages `hmm_baseline/` into the code dataset (it previously shipped only preprocessing/roadgraph/dataset/models/training, so the relabel import would have failed on Kaggle). Notebook is CALIBRATE-then-FULL, resumable via per-shard files.
  - **Go/no-go gate BEFORE any training session:** `relabel_viterbi` prints **differs-from-nearest** — the fraction of fixes where the expert overrides the current geometric target. Near zero means relabeling is a mathematical no-op and the arm must not be run at all. Measured for free on CPU first, deliberately, instead of being discovered after a GPU session.
  - **Written bar for the training arm (set before launch; RESTATED 2026-08-03 at arm-build time).** Continued training from the step-41,499 multi-city ckpt, matched-compute CONTROL (`--viterbi-alpha 0`) vs TREATMENT, same protocol as the IL probe. **Judge TREATMENT against CONTROL, not against the historical 0.8332 / 5.72%** — those are Porto-only single-city IL-probe numbers and the base model here is the 5-city mix, so only the matched-compute contrast is a valid read (the original phrasing of this bar named the historical numbers and would have compared across two different models). Treatment must beat control's Porto held-out match@1 by **>0.5pp**, OR cut control's disconnected-jump% by **>0.3pp** without match@1 regressing more than 0.5pp vs control. Miss on both = H1 dead outright and the hybrid arm is not run.
  - **Local Porto calibration RUN (2026-08-03, local CPU only, serial single core, 300 trajs / 13,077 labeled fixes off the `porto_n1000` cache, 8s) — go/no-go gate CLEARS decisively.** **differs-from-nearest = 34.75%**: the NK expert overrides the current geometric target on better than a third of fixes, so relabeling is emphatically not the mathematical no-op that would have killed H1 for free. Breakdown of how far it moves the target (extra perpendicular distance accepted vs the nearest road, on disagreeing fixes only): **0-1m 38.8%, 1-5m 17.3%, 5-15m 26.8%, 15-50m 17.0%, >50m 0.0%** (median 3.38m, mean 7.10m, p90 20.55m). **56.1% of disagreements sit inside the 5m band tolerant Hit@1 already forgives — those are near-ties where geometry was genuinely ambiguous and topology breaks the tie, exactly the parallel-road/intersection population round-1 Q2 flagged as where sigma-sharpening risks manufacturing confident-wrong labels. The other 43.9% are substantive corrections beyond the tolerance band (= 15.3% of all labeled fixes), and nothing exceeds 50m, consistent with the r=50m candidate radius rather than any runaway.** Interpretation stated honestly: high disagreement proves the target genuinely changes, NOT that the expert is right every time — NK Viterbi is 0.8447 vs the student's 0.8332, better but not dominant, which is exactly why `--viterbi-alpha` blends rather than hard-replaces and why the training bar above still has to be met.
  - **Bug found and fixed by running it** (the reason the calibration was run before any Kaggle time): `np.savez` appends `.npz` to any path not already ending in it, so the shard writer produced `shard.npz.tmp.npz` while the atomic rename pointed at `shard.npz.tmp` — every shard save crashed with `FileNotFoundError`. Fixed by writing through an open file handle, the same pattern `_memmap_cache` already uses.
  - **Kaggle 5-city calibration RUN + COMPLETE (2026-08-03, `alphaevolve-viterbi-relabel-cpu` v1, CPU kernel, zero GPU quota, 300 trajs/city, ~13min total). Gate clears on EVERY city — no city is a no-op.**

    | city:source | labeled fixes | differs-from-nearest | median / p90 extra | <5m / ≥5m |
    |---|---|---|---|---|
    | porto:porto | 13,077 | **34.53%** | 3.46m / 20.60m | 55.8% / 44.2% |
    | tdrive:tdrive | 9,435 | **43.64%** | 4.08m / 17.50m | 54.4% / 45.6% |
    | tdrive:geolife_car | 94,521 | **23.68%** | 2.86m / 11.86m | 64.6% / 35.4% |
    | cabspotting:cabspotting | 23,704 | **44.20%** | 1.98m / 18.33m | 60.6% / 39.4% |
    | romataxi:romataxi | 480,095 | **31.30%** | 1.45m / 12.07m | 70.9% / 29.1% |

    Porto's Kaggle number (34.53%) reproduces the local calibration (34.75%) within sampling noise — independent confirmation across two machines and two different candidate caches (`n1000` local vs `n1658437` Kaggle). **Range across cities is 23.7-44.2%: every city's targets change substantially, and the two sparsest-sampled taxi fleets (T-Drive Beijing 43.6%, Cabspotting SF 44.2%) disagree MOST — consistent with the mechanism, since sparser fixes give per-point geometry less to go on and topology more to correct.** Denser/cleaner geolife_car disagrees least (23.7%), same direction. The <5m share (54-71%) is the ambiguous-tie population everywhere, so the round-1 Q2 concern (sharpening manufacturing confident-wrong labels at parallel roads/intersections) is a real and *general* target, not a Porto artifact.
  - **Process correction worth recording:** the first read of this run concluded "`tdrive:tdrive` errored" because its shard directory was absent from the downloaded output. **That was wrong** — the kernel log shows all 5 cities completed cleanly; the shard was simply missing from an interrupted `kaggle kernels output` download (the first attempt timed out at 5min against a 5GB output). Artifact-absence was treated as evidence of failure without checking the log. Cost was one wrong hypothesis (a cache/row-count mismatch, disproved — all 5 caches match their parquets exactly), no wasted compute. **Lesson, same family as the v2/v3 TPU misdiagnosis already logged: read the actual log before inferring a failure mode from missing artifacts.**
  - **Full-run cost, extrapolated from the calibration's own per-city Viterbi throughput** (total time minus the one-off road-digraph build, which is 308s for Porto alone):

    | city | fixes/s | full pool, 1 core | @4 Kaggle cores |
    |---|---|---|---|
    | porto | 1,635 | 14.0h | 3.5h |
    | cabspotting | 247 | 12.4h | 3.1h |
    | tdrive | 1,179 | 1.5h | 0.4h |
    | romataxi | 7,870 | 0.7h | 0.2h |
    | geolife_car | 8,593 | ~0.0h | ~0.0h |
    | **all 5** | | **28.7h** | **~7.2h** |

    **Fits one 12h Kaggle CPU session with headroom, but treat as optimistic:** a 300-trajectory sample is geographically concentrated, so `RouteDist`'s per-source Dijkstra memo hits far more often than it will across a full city — and that same memo's RAM grows with coverage, which is the real risk at full scale, not wall-clock. **Cabspotting is the outlier at 247 fixes/s, ~35x slower per fix than romataxi** — sparse SF sampling means large `d_gc`, hence a large `2·d_gc + 500m` Dijkstra cutoff exploring much more of a 271,692-segment graph per query. If a full run needs to be trimmed, cabspotting is the first candidate to drop or cap, not Porto.
  - **Throughput (local calibration, warm, single core): ~2,300 fixes/s** (Kaggle's cores measured 1,635 fixes/s on the same city — Kaggle CPU is slower, as expected). Naive extrapolation to Porto's full 82.4M-fix pool: ~10h single-core, so roughly **2.5h on Kaggle's 4 CPU cores** or **~40min on the local 16-core box** — but treat as a floor, not an estimate: a 1,000-trajectory sample is geographically concentrated, so `RouteDist`'s per-source Dijkstra memo hits far more often than it will across the full city, and the memo's RAM grows with coverage. **Hardware note (asked and answered 2026-08-03): TPU and GPU are both worthless for this job** — it is NetworkX Dijkstra plus a Python Viterbi loop with a K×K transition matrix built by nested Python loops, i.e. interpreter-overhead-bound irregular graph traversal with no dense tensor math. Making it TPU-shaped would mean replacing cutoff-bounded local Dijkstra with dense min-plus all-pairs shortest path over 154,099 segments (~95GB fp32, O(n³)) — swapping a near-optimal algorithm for an infeasible one, on a path this project already spent v1-v4 of failed kernels on for a workload that *would* have benefited. The real speedup lever, if the full run ever doesn't fit a session, is vectorizing `log_transition`'s Python double loop into numpy — helps on every hardware option.

  - **Full 5-city relabel LAUNCHED (2026-08-03, `alphaevolve-viterbi-relabel-cpu` v2, CPU kernel, zero GPU quota).** City order `porto → tdrive → geolife_car → romataxi → cabspotting`: Porto first because both eval bars key off it, cabspotting last because calibration measured it at 247 fixes/s (~35x slower per fix than romataxi). `BUDGET_H = 11.0` against the 12h cap; caches read straight from the input mount instead of copied into `/kaggle/working` (the v1 calibration copied ~5GB, burning a quarter of the 20GB output cap and making every later `kaggle kernels output` crawl). **Added a `RouteDist` Dijkstra-memo cap** (`if len(rd.ss) > 20_000: rd.ss.clear()`) — the memo is pure cache so correctness is unaffected, but it grows with geographic coverage and each forked worker keeps its own copy, which was the flagged full-scale RAM risk, not wall-clock. **Cleanup re-gated on a new `ALL_DONE` flag, not `RUN_FULL`** — caught before push: a budget-stopped run has `RUN_FULL=True` and still needs its shards to resume, so the original gating would have deleted hours of finished work.
  - **Full 5-city relabel COMPLETE (2026-08-04, v2, `ALL_DONE=True`, 9.7h wall-clock on 4 Kaggle CPU cores, zero GPU quota). 121.4M GPS fixes carry an expert label; the gate clears at full scale on every city.**

    | city:source | fixes | labeled | differs-from-nearest | (300-traj calib said) | wall-clock |
    |---|---|---|---|---|---|
    | porto:porto | 82,351,446 | 98.9% | **38.81%** | 34.53% | 208.1 min |
    | tdrive:tdrive | 6,271,272 | 82.0% | **36.79%** | 43.64% | 61.6 min |
    | tdrive:geolife_car | 609,227 | 90.2% | **27.70%** | 23.68% | 1.1 min |
    | romataxi:romataxi | 21,164,576 | 94.1% | **30.79%** | 31.30% | 30.3 min |
    | cabspotting:cabspotting | 11,052,959 | 92.9% | **43.22%** | 44.20% | 278.1 min |

    **Calibration was directionally right but not a precise predictor** — it over-called tdrive by 6.9pp and under-called porto by 4.3pp, while romataxi/cabspotting landed within 1pp. Expected: a 300-trajectory sample is geographically concentrated. What survived the 100-1000x scale-up is the thing that mattered: **no city collapses toward 0%, so relabeling is not a no-op anywhere**, and the cost extrapolation held (predicted ~7.2h, actual 9.7h, with cabspotting again the dominant term at 278 min ≈ 48% of the run — exactly the outlier calibration flagged). The RouteDist memo cap did its job: no OOM at full geographic coverage.
    **Label coverage is a per-fix int64 array with -1 for abstains, verified arithmetically from the log alone:** every file is exactly `fixes × 8` bytes (porto 82,351,446 × 8 = 658.8MB, tdrive 50.2MB, geolife_car 4.9MB, romataxi 169.3MB, cabspotting 88.4MB), so row coverage is complete and the loader's `len(vit) < len(df)` staleness guard cannot be tripped by a partial file. Re-verified after download by actually loading all five (mmap, chunked): exact row counts, `int64`, abstain rates matching the log to 3 decimals. Published as private Kaggle dataset **`vineetdairashri/alphaevolve-viterbi-labels`** (927MB, status `ready`). **Abstain rate is the real caveat: 1.1% on Porto but 18.0% on T-Drive** — those fixes silently fall back to the pure-geometry target, so T-Drive's effective treatment dose is ~82% of Porto's, which matters when reading any per-city breakdown of the arm's result.
  - **Real-label integration check RUN LOCALLY (2026-08-04, CPU, `check_real_labels.py`, 1,000 Porto trajs / 46,584 labeled fixes) — PASS, and it found two things neither shipped self-check could.** Both `relabel_viterbi --self-check` and `stage2r --smoke` use FABRICATED labels, so neither could catch a row-misalignment in the 82M-row artifact actually shipped. This one loads the real `viterbi_porto__porto.npy` against a real local df prefix and the real candidate cache, through the real `__getitem__`/`collate_fn` path.
    - **Alignment CONFIRMED decisively: in-candidate rate 99.9936%.** `relabel_viterbi` only ever emits a segment that was among that fix's own candidates, so under correct row alignment essentially every non-abstain label must be found in its own fix's candidate set. **A deliberately misaligned control scores 0.2814%** — the test has enormous teeth, and the real labels pass it. Independent recompute of differs-from-nearest on this subset: 36.15% (kernel reported 38.81% over the full 82.4M-fix pool); labeled share 98.82% vs the kernel's 98.91%.
    - **Correction to an earlier claim: "α=0.5 moves exactly 0.500 mass onto the expert slot" is NOT the general behavior — it is a corner case.** The true invariant is `new[expert] = α + (1-α)·old[expert]`, so the *increase* is `α·(1 - old[expert])`, which equals α only when geometry gave the expert ~zero mass. `stage2r --smoke` asserts `moved > 0.4` on a synthetic case that pins the expert to the FARTHEST candidate — exactly that corner. **On real data the mean geometric mass already sitting on the expert slot is 0.4280, so the mean increase is 0.2860, not 0.5.** The blend code is correct (verified exact to 0.00e+00 on `new[expert] = α+(1-α)·old`, on non-expert slots being exactly `(1-α)·old`, on rows summing to 1, and on abstain/no-hit rows being bit-unchanged); it was the *description* of the dose that was overstated.
    - **Attribution confound found, worth knowing BEFORE reading the arm's result.** σ=7.0 is held constant across both arms, so the only difference is the α blend — but that blend is not purely relabeling. **On the 63.8% of fixes where the expert AGREES with the nearest road, the blend simply piles more mass onto the slot geometry already picked, i.e. it acts as extra sharpening *beyond* σ=7 — the same lever already shipped as σ 10→7. Only 36.2% of fixes are true relabeling** (n=46,581). So if TREATMENT wins, the win is not automatically attributable to topology-aware relabeling. **Clean disambiguation if it wins: a third arm at `--viterbi-alpha 0` with σ lowered further (≈5.0), matching the entropy reduction without the topology information.** If that third arm captures most of the gain, the result is sharpening, not DAgger.
  - **Training-arm kernel BUILT and push-ready (2026-08-03, `notebooks_relabel_arm/`, `alphaevolve-relabel-arm-t4`, T4x2) — blocked only on the labels landing + being uploaded as `alphaevolve-viterbi-labels`.** Two branches from the same step-41,499 multi-city ckpt, **2.2h each** (user capped the session at 4.8h total; `--max-hours` starts AFTER the loaders are built at `stage2r.py:548`, so it measures pure training time and pip/mounts/smoke/two 5-city loader builds/cleanup are all on top — ~0.4h reserved, 4.4h split evenly), identical argv except `--viterbi-alpha` (0.0 control / 0.5 treatment). α=0.5 and not something timid on purpose: if half the target mass on the expert moves nothing, a smaller α moves nothing either; α=0.3 is the follow-up only if 0.5 overshoots (match@1 drops while jump% falls).
    - **Design blocker found and solved at build time: the base ckpt sits at step 41,499 of a 41,500-step cosine, so a plain `--resume` would have trained both arms at LR ≈ 0 and produced two no-op checkpoints and a meaningless null result.** Both arms therefore use `--constant-lr --lr 3e-5 --steps 60000` — the same escape hatch the P0 LR-tail probe used (`--constant-lr` forcibly resets `param_groups[lr]` and discards the saved cosine state on resume, verified in `train_multi` at `stage2r.py:512-534`), applied identically to both arms so it stays single-variable. `--max-hours` is the binding constraint; `--steps` is only headroom.
    - **Caches symlinked, not copied,** into each arm's `out-dir/cache/{city}__{source}/`: the dataset only ever reads that npz (its memmap scratch goes to `CAND_MMAP_DIR`=`/kaggle/temp`, which is not output-capped), so two arms copying ~5GB each would have eaten half the 20GB working cap for files deleted at the end anyway. Both label and cache staging use `rglob`-by-filename and **assert before the GPU spins up**, per the v4 mount-nesting postmortem.

  - **ARM RUN COMPLETE + EVALUATED (2026-08-04, kernel `alphaevolve-relabel-arm-t4` v2, both arms). VERDICT: TREATMENT PASSES the written bar on the jump% leg.** Both arms resumed cleanly from step 41,500 at constant LR 3e-5 (logs confirm `constant-lr, sched reset`, so the cosine-tail blocker was genuinely avoided).
    - **The arms are NOT step-matched, and this was a design miss on my part.** `--max-hours 2.2` capped *wall time*, but the treatment pays an extra per-step cost for the Viterbi label lookup + blend, so control reached step 44,540 (3,040 steps) while treatment reached only 43,961 (2,461 steps) — treatment got **19% fewer gradient steps**. Comparing the two *final* checkpoints therefore confounds the treatment with less training. Salvaged for free because both arms checkpoint every 500 steps: **step43500 exists on both and is the exact step-matched pair.** All headline numbers below are step-matched; final-vs-final is reported only as a secondary read. **Lesson for the next A/B: cap arms by `--steps`, not `--max-hours`, whenever the arms have different per-step cost.**
    - **Step-matched results (step 43,500; Porto held-out 500 trajs / 20,411 fixes, `eval_research2.py`; jump% from `diag_jump_vs_priorerror.py`, same split, 20,129 scored fixes):**

    | metric | CONTROL α=0.0 | TREATMENT α=0.5 | Δ | significance |
    |---|---|---|---|---|
    | match@1 (road head, online) | 0.8528 | 0.8524 | −0.04pp | z=0.11, p=0.91 (null) |
    | **jump% (hyb+topo, unconditional)** | **5.83%** | **4.85%** | **−0.98pp** | **z=4.31, p=1.6e-05** |
    | match@1 (hyb+topo, in diag) | 0.8304 | 0.8294 | −0.10pp | n.s. |
    | hit@1 (prior rollout) | 0.664 | 0.648 | −1.6pp | — |
    | hit@5 | 0.564 | 0.574 | +1.0pp | — |
    | hit@15 | 0.558 | **0.607** | **+4.9pp** | z=10.04, p=1.0e-23 |

    **Bar was: beat CONTROL by >0.5pp match@1, OR cut CONTROL's jump% by >0.3pp without match@1 regressing >0.5pp. Leg 1 fails (match@1 is a flat null, 0.04pp). Leg 2 passes decisively: jump% falls 0.98pp (3.3x the required margin, p=1.6e-05) while match@1 regresses 0.04pp (road head) / 0.10pp (hyb+topo), both far inside the 0.5pp tolerance.** So the expert relabeling does not make the matcher *pick better roads*, it makes it pick *more connected* ones — which is exactly the failure mode DAgger-style expert relabeling was hypothesized to fix, and exactly what a topology-aware Viterbi expert should transfer. H1 survives.
    - **Unrequested bonus, and the largest single effect in the table: path prediction at long horizon. hit@15 +4.9pp (0.558 → 0.607, p=1.0e-23), hit@5 +1.0pp, hit@1 −1.6pp.** The sign flip across horizons is coherent — the expert's targets are sequence-optimal rather than per-fix-optimal, so they trade a little immediate accuracy for trajectories that stay on a plausible route much further out. Prediction was not part of the bar; it may be the more valuable result.
    - **Training loss was useless for this A/B, as predicted pre-launch.** Treatment's `road` reads 1.00 vs control's 1.10, which looks like a decisive win and is an artifact: α=0.5 lowers the soft target's own entropy, so treatment gets a lower CE for free by construction. The `road_ex = road − tgt_ent` fix that makes the arms comparable was written but deliberately not pushed (would have restarted the run). **Any future α-varying arm MUST log `road_ex`, or its training curves are unreadable.**
    - **The attribution confound logged above is now live and unresolved.** Treatment won, so the pre-registered disambiguation is warranted: **a third arm at `--viterbi-alpha 0` with σ≈5.0**, matching the entropy reduction without the topology information. If that arm captures most of the −0.98pp jump% gain, this is sharpening (the already-shipped σ lever), not DAgger. Until it runs, the honest claim is "the α blend cuts disconnected jumps", not "expert relabeling cuts disconnected jumps".
    - Eval tooling made portable in passing: `eval_research2.py` gained `--ckpt-dir`/`--ckpt-prefix` (it globbed `stage2r_porto_step*.pt` and would have silently matched **zero** of these `stage2r_multi_*` checkpoints), and both it and `diag_jump_vs_priorerror.py` now honour an `AE_BASE` env override so they run off-box; both ship in the code dataset. Arm checkpoints + the held-out candidate cache are archived as private Kaggle dataset **`vineetdairashri/alphaevolve-arm-ckpts`** (220MB, `ready`).

  - **Zero-GPU disambiguation attempt (2026-08-04, `diag_expert_subset.py`, local, both step-43,500 ckpts, Porto held-out 500 trajs / 19,594 jump-scored fixes) — the evidence LEANS AGAINST the topology-transfer story.** Kaggle GPU quota is exhausted, so the causal third arm (task #29) is blocked; this substitutes a free observational test on the checkpoints already in hand. Partition the held-out fixes by the expert's own flag — `differ` = expert relabeled the fix (carries topology information), `agree` = expert picked the same segment the nearest-road target already had (blend acts as pure extra sharpening) — and ask where the treatment's jump reduction lands.

    | bucket | n | control jump% | treatment jump% | Δ | rel. | p |
    |---|---|---|---|---|---|---|
    | differ (relabeled) | 7,868 | 6.012% | 4.715% | −1.30pp | −21.6% | 3.1e-04 |
    | agree (sharpening only) | 11,726 | 5.620% | 4.521% | −1.10pp | −19.6% | 1.2e-04 |

    **The reduction is essentially uniform. Difference-in-differences = 0.20pp, z=0.43, p=0.67 — no evidence whatsoever that the win concentrates on the fixes the expert actually relabeled.** The strong DAgger claim predicted concentration on `differ`; it is not there. **Read honestly, this is not proof of sharpening** — an alternative that survives is that relabeling teaches a *global* "prefer connected continuations" behaviour that generalizes to every fix, which this test cannot exclude. What it does do is remove the main positive evidence for fix-local topology transfer, and it raises the prior that the σ-only arm will reproduce most of the −0.98pp. **One fact still cuts the other way and should not be swept aside: hit@15 +4.9pp is a large long-horizon prediction gain, and plain target sharpening is a weak explanation for it.**
    - Alignment was verified before reading anything: the attached label slice independently reproduces the kernel's own Porto statistics — abstain 0.73% (kernel 1.1% over the full pool) and differ-share 38.5% on the 60-traj smoke (kernel 38.81%). Two real bugs were caught building it: (1) `ev._loader()` filters the df and **resets the index** while the label .npy is indexed over the full 82.4M-row pool, so attaching the raw file would have read trajectories ~0–250 instead of 200000–200500 — and the dataset's `len(vit) < len(df)` staleness guard **cannot** catch this, since 82.4M > 20k; fixed by slicing on recomputed original row positions. (2) the label file is a row *prefix* (82,351,446 of 82,351,535 — the relabel ran with `--limit-trajs 1658437`), so a full-coverage assert false-alarms; the correct guard is that the rows this eval touches are inside the prefix.

  - **ROUND-2 CONTINUATION RUN COMPLETE + EVALUATED (2026-08-18, kernel `alphaevolve-relabel-arm2-t4` v1, `notebooks_relabel_arm2/`). VERDICT: CONFIRMED-STABLE — the round-1 effect is real and durable, but it does NOT grow with more training.** Round 1's step-matching was salvaged after the fact; this run made it structural. Both arms resumed from their OWN step-43,500 checkpoint (not the round-1 finals, which carried the unequal-compute confound) and were capped by `--steps 48000` rather than wall-clock, so both landed on exactly 4,499 additional steps — **1.83x the treatment's round-1 budget of 2,461.** Session ran 7.08h inside a 9h/8.5h cap (control 3.41h, treatment 3.67h); neither arm hit `--max-hours`, so both exited on the step target and the comparison is clean by construction. The treatment's dynamic max-hours clamp was computed (4.44h vs the 4.5h static plan) but never bound.
    - **Step-matched results (step 47,500 vs round 1's step 43,500; Porto held-out 500 trajs / 20,411 fixes, `eval_research2.py`; jump% + hyb+topo match@1 from `diag_jump_vs_priorerror.py`, 20,129 scored fixes / 19,594 jump-scored):**

    | metric | R1 CTL @43.5k | R1 TRT @43.5k | R2 CTL @47.5k | R2 TRT @47.5k | R2 Δ (TRT−CTL) |
    |---|---|---|---|---|---|
    | **jump% (hyb+topo)** | 5.87% | 4.92% | 5.89% | **4.71%** | **−1.18pp** |
    | match@1 (hyb+topo) | 0.8290 | 0.8298 | 0.8326 | 0.8283 | −0.43pp |
    | match@1 (road head) | 0.8524 | 0.8539 | 0.8523 | 0.8462 | −0.61pp |
    | hit@1 (prior rollout) | 0.650 | 0.654 | 0.634 | **0.674** | +4.0pp |
    | hit@5 | 0.605 | 0.561 | 0.582 | 0.596 | +1.4pp |
    | hit@15 | 0.600 | 0.605 | 0.581 | 0.574 | −0.7pp |

    **Bar was three-way: CONFIRMED-GROWING (jump gap widens past 1.3pp, match@1 not regressing >0.5pp) / CONFIRMED-STABLE (gap holds 0.6–1.3pp, match@1 within 0.5pp) / FAILED (gap <0.5pp or match@1 regresses >0.5pp). Result: jump gap 0.95pp → 1.18pp (+0.23pp), match@1 −0.43pp on the decode the jump number comes from. That is CONFIRMED-STABLE.** Per the bar's own STABLE branch: ship α=0.5, stop buying steps for this lever.
    - **The honest caveat on which match@1 the bar reads.** The two decodes disagree about the regression clause: hyb+topo (the deployment config, and the *same* decode the jump% is measured under) gives −0.43pp and passes; the bare road head gives −0.61pp and would trip the >0.5pp clause. Pairing jump% with the match@1 from the same decode is the only internally coherent read of a trade-off statement, so CONFIRMED-STABLE stands — but the road-head number is genuinely on the wrong side of the line and should not be buried. **Both leaned worse than round 1, where the treatment was +0.08pp / +0.15pp. The sign flipped.**
    - **Noise floor, measured rather than assumed.** Re-running round 1's step-43,500 pair under the same harness reproduced the doc's numbers to within 0.04–0.15pp (`match@1` head 0.8524/0.8539 vs the logged 0.8528/0.8524; jump 5.87%/4.92% vs the logged 5.83%/4.85%). Eval is stochastic — `straight_through_sample` draws — so **~0.15pp is the run-to-run floor for match@1 and ~0.05pp for jump%.** The −0.61pp road-head regression is ~4x the floor and therefore real; the +0.23pp jump-gap widening is ~4x the jump floor but small in absolute terms, which is exactly why it reads as STABLE and not GROWING.
    - **More training bought the control nothing and the treatment very little.** Over 4,000 extra steps the control's jump% moved 5.87% → 5.89% (flat) and the treatment's 4.92% → 4.71% (−0.21pp). The α=0.5 blend converges to a modestly better jump rate and then sits there; the lever is saturated, not scaling. **This is the finding that kills "just train the arm longer" as a next step.**
    - **Path prediction is where round 2 disagrees with round 1, and not in a way that is safe to spin.** Round 1's headline prediction bonus was hit@15 +4.9pp; at round 2 the long-horizon gain is gone (−0.7pp) and the gain has moved to hit@1 (+4.0pp, the best hit@1 of all four checkpoints). Short-horizon and long-horizon prediction gains swapped places between rounds. Given the measured noise floor on the online metrics, these rollout numbers (500 trajs, stochastic decode) are **not** established as stable across runs, and the correct reading is that the prediction effect is real in magnitude but unstable in *which horizon* it lands on. Any claim about a specific horizon needs repeat seeds first.
    - **What this does and does not unlock.** The hybrid-expert arm was gated on "this one lifting" — it did lift on the jump leg, twice, so the gate technically opens. But STABLE plus a saturating trend plus a match@1 sign flip is a weak mandate for spending quota on a *stronger* expert whose exogeneity is already compromised (hybrid emission is the student's own road head). **The pre-registered disambiguation (task #29, `--viterbi-alpha 0` with σ≈5.0) remains the higher-value next spend**, and `diag_expert_subset.py`'s difference-in-differences null (0.20pp, p=0.67) still says the jump win is not concentrated on relabeled fixes. Until #29 runs, the claim stays "the α blend cuts disconnected jumps by ~1.2pp and holds there", not "expert relabeling cuts disconnected jumps".
    - Artifacts: checkpoints step44000–47500 + finals for both arms at `$CLAUDE_JOB_DIR/tmp/arm2_out/`, kernel log alongside. Notebook + metadata in `.worktrees/research2/notebooks_relabel_arm2/`.

**Product roadmap items (3)–(5) — deprioritized, not abandoned:** cheap accuracy is still on the table (P1 beat its bar ~2x on quota-based, not scarce, Kaggle compute) — revisit once the data ladder or P2 flattens.

## 6. Workspace layout (cleaned 2026-07-11: keep-what-worked sweep)

**Active worktrees (all load-bearing, do not retire):** `research2` (winning WM track + `matcher.py` package + evals), `HMM_baseline` (NK-HMM fallback; `baseline_hmm.py` imported by matcher), `data-preprocess` (dataset/roadgraph/preprocessing modules imported everywhere), `kaggle` (`ckpt/` = stage0/stage1 checkpoints + eval caches — the model store).

**Retired worktrees (checkouts removed; full code record stays on their git branches):** `evaluation` (Track-A eval harness), `Stage2_kaggle` (Track-A Run-3 site; its untracked Run-3 ckpts moved to `research/archive/worktree_artifacts/Stage2_kaggle/`), `stage2_new` (Track-A stage-2 era).

**Archived inside research2** (`archive/`): Run-1 ckpts + eval txts, Run-3 log, Track-A eval scripts, `stage2_correction.md`, graphify output. Note: `models/world_model.py` and `training/stage2.py` are Track-A code but LOAD-BEARING (world_model2/stage2r/stage3 import from them) — kept in place.

## 7. Standing conventions

Every run gets a written success bar BEFORE launch (caught every bug so far). **Compute permission (user decision 2026-07-12, inverted): Kaggle needs NO permission — run freely (idle quota wasted otherwise), but keep runs resume/pause-able (`--max-hours`+`--resume`+`--ckpt-every`) and notify user to save output / save it yourself; LOCAL runs now need permission — ask first.** Literature: check `literature_papers/*.json` (48 entries) before any web search; classify new papers against P1a/P1b/P2/P3; DeepMM, RouteKG, Ayara lineage are the fixed comparison anchors; any system covering 3+ pillars → surface immediately.
